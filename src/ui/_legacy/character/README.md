# 角色域归档件

这里保留角色详情态改版前、当前全项目零引用的组件。

| 目录 | 来历 |
| --- | --- |
| `EquipmentDrawer/` | 旧装备 tab 的仓库抽屉，已由详情态立绘位上的 `CharacterDetailView/EquipPicker` 取代。 |
| `DeckForgeBar/` | 旧卡组页三按钮操作条，已由 `character/DeckForgeHub` 取代。 |

`EquipPanel` 与对应样式已删除，改由 `CharacterDetailView/Workbench/ProfilePanel` 组合属性表和装备槽。

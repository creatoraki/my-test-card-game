// 二连箭(twin-arrow)配色表 —— 调色只改这里, 绘制层不写死任何颜色。
//
// 冷暖分工沿用美术原型: 弓是暖金光团(施法者侧的能量), 箭是青白光(飞行中的锐利感),
// 命中是橙黄火花 + 暖白冲击环(撞击的热量)。弓与箭不同色, 两段之间的「交接」才读得出来。
//
// 注意: 弓的每一层都是半透明 + lighter 叠加 —— 纯光团造型不允许出现任何不透明硬边,
// 所以这里没有实体描边色(原型的 #b98b2e 实木与米白高光已整条移除)。

export const BOW_COLORS = {
  /** 最外层辉光: 极宽极淡, 让弓「化」在空气里, 不至于读成一根霓虹灯管。 */
  bloom: "rgba(255, 172, 66, 0.07)",
  /** 金雾: 宽、淡, 负责「光在发散」的体积感。 */
  haze: "rgba(255, 190, 90, 0.2)",
  /** 主体暖金弧。 */
  core: "rgba(255, 200, 110, 0.62)",
  /** 乳白内芯: 弓的亮度骨架 —— 刻意不给满不透明, 免得又出现硬边。 */
  inner: "rgba(255, 246, 216, 0.72)",
  /** shadowBlur 用的辉光色。 */
  glow: "rgba(255, 186, 92, 0.95)",
  /** 弓弦: 细光丝, 比弓臂更淡。 */
  string: "rgba(255, 238, 205, 0.6)",
  /** 弓梢能量结的径向渐变(内 → 外)。 */
  tipKnot: ["rgba(255, 246, 216, 0.95)", "rgba(255, 184, 84, 0.45)", "rgba(255, 140, 30, 0)"],
  /** 弓心气场: 随拉弓变亮的大范围柔光。 */
  aura: ["rgba(255, 214, 140, 0.34)", "rgba(255, 160, 60, 0.14)", "rgba(255, 120, 0, 0)"],
  /** 蓄力球(搭箭点): 原型同款三档。 */
  charge: ["rgba(255, 245, 200, 0.9)", "rgba(255, 175, 70, 0.45)", "rgba(255, 120, 0, 0)"],
} as const;

export const ARROW_COLORS = {
  /** 箭体光团的径向渐变(白芯 → 青 → 透明)。 */
  body: ["rgba(255, 255, 255, 0.95)", "rgba(140, 234, 255, 0.55)", "rgba(90, 200, 255, 0)"],
  /** 箭杆线性渐变(尾 → 头)。 */
  shaft: ["rgba(140, 234, 255, 0)", "rgba(190, 244, 255, 0.75)", "rgba(255, 255, 255, 0.95)"],
  /** 拖尾线性渐变(尾 → 头), 与原型一致。 */
  trail: ["rgba(110, 215, 255, 0)", "rgba(150, 232, 255, 0.35)", "rgba(225, 250, 255, 0.95)"],
  /** 辉光色。 */
  glow: "rgba(125, 233, 255, 0.95)",
  white: "#ffffff",
} as const;

export const IMPACT_COLORS = {
  /** 冲击环外圈(暖白金)与内圈(纯白), alpha 由绘制层按生命周期填。 */
  ringOuter: (alpha: number) => `rgba(255, 215, 150, ${alpha.toFixed(3)})`,
  ringInner: (alpha: number) => `rgba(255, 255, 255, ${alpha.toFixed(3)})`,
  /** 命中光爆径向三档。 */
  burst: ["rgba(255, 240, 210, 0.95)", "rgba(255, 150, 70, 0.55)", "rgba(255, 60, 0, 0)"],
  /** 火花: 色相由粒子自带(25–60 的橙黄区间), 亮度随寿命升高。 */
  spark: (hue: number, alpha: number) =>
    `hsla(${hue.toFixed(0)}, 100%, ${(58 + alpha * 35).toFixed(0)}%, ${alpha.toFixed(3)})`,
} as const;

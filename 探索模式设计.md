# 探索模式设计：失真路由图

> 状态：**设计定稿，尚未按本方案实现。**
>
> 本文以「阿弥陀签式路由决策」为探索层骨架。以下机制已在本版中**废弃**，代码与旧文档中若仍存在，均以本文为准：
>
> - **探索事件卡牌局**（事件卡库、抽牌权、路线牌、横向轨迹）——被路由图取代。
> - **区域危险度**——被**净化粒子**取代，难度轴只保留一条。
> - **残片**——被**实物战利品 + 城市居民积分**取代，探索层不再产出抽象货币。

---

## 一、设计目标

### 1.1 核心体验

玩家接入一座长期失修、不断重排的城市维护网络。每次接入前，系统会短暂展示一张由入口、线路和终点构成的失真路由图；玩家选择一个入口后，信号会沿线路自动下行，遇到横线时必须切换至相邻线路，最终抵达一个事件终点。

玩家知道每个终点是什么事件，却不能在隐藏线路后完全确定入口会通向哪里。核心问题是：

> 在这次远征状态下，我最需要抵达哪个事件？我是否记住了通向它的线路？要不要消耗有限的探索指令来确认或改写结果？

玩法的紧张感来自短时信息、远征资源和角色能力的共同管理，而非纯粹拼手速或视觉记忆。

### 1.2 一次远征的结构

一张地图由 **5 至 7 段独立的路由决策关卡**构成。每段走完一次完整的 `A-E -> 1-5` 路线后，只结算一个事件；随后生成下一段全新的入口、线路与事件终点。

```text
生成本段 5 个终点事件
        ↓
完整线路显示约 1 秒
        ↓
线路隐去，玩家选择入口 A-E（此阶段可开背包）
        ↓
信号自动沿线路下行，遇横线强制换线
        ↓
抵达 1-5 的某个事件（此时**什么都还没发生**）
        ↓
落点浮层：在该事件的两条分支里选一条（战斗终点也走这一步，此阶段可开背包）
        ↓
按所选分支结算：扣能量 / 跑效果 / 进战斗 / 立即撤离
        ↓
未结束：扣除本段净化粒子，生成下一段路由图
结束：BOSS / 撤退 / 远征结算
```

标准地图建议为 6 段；短地图为 5 段，深层或高难地图可扩展至 7 段。

### 1.3 三根承重柱

新玩法的全局层由三样东西撑住，缺一不可：

- **队伍 HP 跨段继承**：医疗、避战、撤退与补给终点才有真实价值。
- **净化粒子**：唯一的难度与时限轴。它只会随远征推进而下降，档位越低敌人越强、我方污染越重、掉落越好。
- **实物战利品与背包**：探索层产出的是**占背包格的实物**。背包越满，全队命中/暴击/闪避越低；团灭时背包全丢。撤退因此是「把已经背在身上的东西真的带回去」，而不是一个失败按钮。

经验与战利品刻意不对称：**经验在每场战斗后即时入账，谁也拿不走**；实物必须活着回到据点才算数。

---

## 二、单段路由规则

### 2.1 图形结构

基础路由图使用 5 条竖线、5 个入口和 5 个终点：

```text
入口：    A     B     C     D     E
          |     |-----|     |     |
          |-----|     |     |-----|
          |     |     |-----|     |
          |     |-----|     |     |
终点：    1     2     3     4     5
```

- 信号从所选入口自上而下移动。
- 遇到横线时，信号必须横移到相邻竖线后继续向下。
- 一张图的线路必须是合法阿弥陀签：横线仅连接相邻竖线，且同一高度不得共享端点。
- 每个入口必定且仅会抵达一个终点；每个终点也恰好对应一个入口。
- 默认所有终点事件可见；中间线路在展示结束后隐去，入口标签和终点事件继续可见。

### 2.2 观察与选择

1. 系统生成 5 个终点事件和一张对应线路图。
2. 完整线路默认显示 **1.0 秒**，显示期间不可选择、**不可打开背包**。
3. 线路隐藏后，玩家不限时选择 A-E 的任意入口；此阶段可自由开背包整理。
4. 玩家确认后，信号沿真实线路移动；移动动画逐段显示实际经过的竖线与横线。
5. 抵达终点后，结算该事件，记录本段结果，并进入下一段或结束远征。

基础展示时长随地图和进度调整，但不应将难度简单堆到极短时间：

| 阶段 | 横线数建议 | 默认展示时长 |
| --- | ---: | ---: |
| 前段 | 4-6 | 1.2 秒 |
| 中段 | 6-9 | 1.0 秒 |
| 后段 | 9-12 | 0.8-1.0 秒 |

### 2.3 事件结算

一次走线只触发一个终点事件。事件可能立即结算，也可能进入战斗、商店或特殊交互；结算完成才生成下一段线路。

每个事件用 `energyDelta`（净化粒子增减）而非旧的 `dangerDelta` 描述代价。

#### 2.3.1 落点确认与分支选择（`landed` 阶段）

**抵达 ≠ 结算。** 走线动画播完后，会话进入 `landed`：落点已定、效果一件都没生效，界面弹出落点浮层，给出该事件的**两条分支**，玩家选定后才真正扣能量、跑效果。

- **所有终点都走这一步，战斗也不例外**——普通战、精英、BOSS、撤离升降机全部先弹浮层。「还没看清落在哪张卡上就已经在战斗页里了」是不可接受的。
- 分支写在事件数据的 `choices` 上（`EventChoice`：`label` / `desc` / `energyDelta` / `effects`）。**代价与效果只认被选中的那一支**；事件自身的 `energyDelta` 退化为终点卡上的**预览值**，必须与 `choices[0]` 一致。
- 分支设计的硬规则：`choices[0]` 是主选项（保留原有平衡），`choices[1]` 是替代路线且**一律有代价**——要么花净化粒子规避（战斗类走 `EXPLORE_RULES.bypass`，代价刻意高于硬吃这场战斗的能量消耗），要么少拿收益。绝不允许出现「白给的第二选项」，那等于取消了这个终点的风险。
- `landed` 与 `resolving` 同属「不限时、等玩家操作」的阶段，允许开背包；但**不允许**在此直接撤离远征或跳到下一段。

建议每段的五个终点至少覆盖不同决策维度，而不是简单地排列五档数值奖励：

| 类别 | 例子 | 主要价值 |
| --- | --- | --- |
| 生存 | 医疗站、净化站、护盾修复 | 降低后续团灭风险 |
| 成长 | 模组材料、装备、经验 | 提高长期收益 |
| 战斗 | 普通战斗、精英战、伏击 | 以风险换经验与稀有掉落 |
| 经济 | 商人、量子售货机、投递口 | 将既有资源转换为所需资源 |
| 路由 | 情报节点、稳定器、短接器 | 改变后续线路的信息或操作优势 |
| 能量 | 净化机、隐匿通道、通风破口 | 直接改写难度曲线的位置 |
| 风险 | 掉血、debuff、资源损失、路由干扰 | 负面结果，或高风险高收益 |

事件生成保底规则：

- 每段至少 1 个生存或低风险终点。
- 每段至少 1 个成长终点。
- 每段至少 1 个战斗或高风险终点。
- **同一段最多 1 个纯负面终点**；纯负面与高风险合计**不超过 2 个**。
- 「无生存终点」与「负面终点达到 2 个」不得同时成立。
- 精英、BOSS、高额掉落和大额能量变动仅在中后段出现。

---

## 三、远征节奏与终局

### 3.1 推荐的六段地图

| 段数 | 设计职责 | 事件倾向 |
| --- | --- | --- |
| 第 1 段 | 建立方向 | 基础物资、轻度战斗、少量补给 |
| 第 2 段 | 首次分流 | 商人、普通战斗、路由资源 |
| 第 3 段 | 风险抬升 | 高收益、负面事件、恢复 |
| 第 4 段 | 中局压力 | 精英、能量控制、强掉落 |
| 第 5 段 | 收束选择 | BOSS 接入、撤离、最后补给、宝库 |
| 第 6 段 | 终局 | BOSS、撤退或最终高风险奖励 |

不要将最后一段机械固定为 BOSS。建议在第 4 或第 5 段后让 BOSS 接入网络，使后续终点池可能出现：

- BOSS 接入点；
- 撤离升降机；
- 最后补给；
- 高风险宝库；
- 隐匿路线。

这样玩家能够带着已获得的战利品撤离、立刻挑战 BOSS，或为了更好的状态与收益再进行一段路由决策。

### 3.2 远征结束方式

| 结束方式 | 背包 | 经验 | 说明 |
| --- | --- | --- | --- |
| BOSS 胜利 | 全部带回 + BOSS 掉落 | 已即时入账 | 通关 |
| 主动撤退 | 全部带回 | 已即时入账 | 不算失败 |
| 团灭 | **背包全部遗失** | 已即时入账 | 角色回据点复原 |

**已通过投递口寄回的物品不受团灭影响**，这是背包玩法唯一的保险手段（见 §六）。

---

## 四、净化粒子

### 4.1 职责

净化粒子是**唯一的难度轴与时限**。它描述的是「过滤装置还能撑多久」，而不是「城市被惊动到什么程度」——旧的区域危险度已废弃，不得再引入第二条并行难度数值。

它的特点是**单向下降 + 局部可干预**：

- 每段固定消耗，玩家无法阻止 → 远征本身就是一条自动抬升的难度曲线。
- 少数事件可以补充或额外消耗它 → 玩家仍能主动选择「提前变强变富」或「花钱买安全」。

### 4.2 消耗与档位

起始能量 **100**（据点「过滤装置充能台」可升级上限）。

| 消耗来源 | 变化 |
| --- | ---: |
| 每完成一段 | −10 |
| 终点为普通战斗 | 额外 −4 |
| 终点为精英战 | 额外 −7 |
| 终点为 BOSS | 额外 −10 |

> 首图 6 段 + 约 3 场战斗 + 1 场精英 ≈ 消耗 82，恰好从 1 档滑到 4/5 档。**首图必须让玩家亲身撞到时限。**

| 档 | 能量 | 名称 | 敌人变化 | 我方污染层数 | `K_energy` |
| ---: | ---: | --- | --- | ---: | ---: |
| 1 | 80-100 | 充盈 | 基础 | 0 | 1.00 |
| 2 | 60-79 | 稳定 | 力量 +1 | 0 | 1.25 |
| 3 | 40-59 | 衰减 | 力量 +1、追加 1 名敌人 | 1 | 1.55 |
| 4 | 20-39 | 告急 | 力量 +2、追加 1 名敌人、行动更快 | 2 | 1.90 |
| 5 | 0-19 | 枯竭 | 力量 +3、追加 2 名敌人、行动更快 | 3 | 2.40 |

**污染层数**（每层叠加，本次远征内不可自行清除，需净化类事件）：

- 全队受到伤害 **+6%**；
- 治疗效果 **−10%**。

> 选择削弱我方而不是再砍最大 HP，是因为 HP 已经跨段继承，再砍上限会把失败提前锁死。

**枯竭档保护**：能量进入第 5 档后，**撤离升降机必定出现在此后每段的终点池中**。时限是压力，不是死刑；玩家永远有把背包带回去的机会，代价是放弃这一段的收益。

### 4.3 玩家对能量的主动干预

| 方向 | 例子 | 效果 |
| --- | --- | --- |
| 买安全 | 逆流净化机 | E +25，但本段掉落全部作废 |
| 买安全 | 隐匿通道 | 免除本段的 −10 基础消耗 |
| 买收益 | 强拆配电柜 | E −8 → 模组材料 ×2 |
| 买收益 | 通风破口 | E −12 → 装备 ×1（品质按当前档位） |

### 4.4 BOSS 缩放

BOSS 以开打时的能量档位作为额外缩放依据：

- BOSS `maxHp` 乘以 $1 + 0.15 \times (档位 - 1)$。
- 第 4 档起追加 1 名护卫。
- BOSS 掉落走统一的掉落系数（见 §五），不再单列倍率。

BOSS 接入点出现后，界面应实时预览当前档位下的 BOSS 强度与掉落，帮助玩家决定现在挑战还是继续探索。

---

## 五、掉落系数与挑战词条

### 5.1 统一系数

全局只有一个掉落系数，**同时作用于掉落率、掉落品质与经验值**：

```text
K = K_energy × (1 + Σ 挑战加成) × K_global
```

- `K_global` 是集中在 `explore/rules.ts` 的总旋钮，默认 `1.0`。调整整体产出只动它。
- `K_energy` 见 §4.2。
- 挑战加成见 §5.3，**单场合计上限 +150%**。

**作用方式**：

| 对象 | 算法 |
| --- | --- |
| 掉落率 | `finalChance = base × K`。结果 >1 时，整数部分为保底件数，小数部分再掷一次（`1.6` = 必掉 1 件 + 60% 再掉 1 件） |
| 经验值 | `finalExp = baseExp × K` |
| 掉落品质 | 由 K 映射出 `qualityBias`，用于稀有度权重右移 |

`qualityBias` 映射与权重表：

| K | bias | 常见 | 精良 | 稀有 |
| --- | ---: | ---: | ---: | ---: |
| ≤1.2 | 0 | 85% | 14% | 1% |
| 1.2-1.8 | 1 | 75% | 22% | 3% |
| 1.8-2.5 | 2 | 65% | 30% | 5% |
| 2.5-3.5 | 3 | 52% | 38% | 10% |
| >3.5 | 4 | 40% | 45% | 15% |

> 同一张掉落表，低档时掉一堆破烂，高档时掉同类里的好货。这比单纯翻倍数量更能诱惑玩家往下走。

### 5.2 敌人掉落表

`EnemyDef` 增加 `dropTable: DropEntry[]`，每条 `{ itemId, chance, min, max }`。经验**不进掉落表**，仍是敌人固定值。

首图示例：

| 敌人 | 掉落 |
| --- | --- |
| `scrap-bot` 清运机械 | 废件 60% ×1-2 ／ 磨损伺服器（材料）25% ／ 破损装甲板（防具）8% |
| `radio-bot` 信标机 | 废件 40% ／ 信号线圈（材料）30% ／ 数据存档 12% ／ 干扰器（饰品）6% |
| 报废压缩机（精英） | 材料 100% ×2 ／ 装备 35% ／ 数据存档 40% |
| 首图 BOSS | 装备 100%（精良起） ／ 材料 ×3 ／ 专属模组材料 100% |

### 5.3 挑战词条

**规则**：

- **自动生效**。每场战斗随机附带 **1-2 条**词条，开战瞬间公示，之后不可更换。
- **无惩罚**。未完成只是拿不到加成，不扣任何东西。
- 多条加成**相加**后并入 §5.1 的 `K`。
- 词条是「机会型加成」：玩家看到词条后临场调整打法，而不是事前下注。

首图词条池：

| 词条 | 条件 | 加成 |
| --- | --- | ---: |
| 镜像舞者 | 每回合只打出同一费用的卡牌 | +40% |
| 同步 | 在同一回合内击杀所有敌人 | +60% |
| 速决 | 3 回合内结束战斗 | +50% |
| 静默 | 全程不使用治疗与护盾牌 | +35% |
| 节流 | 每回合结束时至少剩余 1 点光 | +25% |
| 单核 | 全程只由一名角色打出卡牌 | +45% |
| 满载 | 开战时负重 ≥ 20 格 | +30% |
| 无伤 | 无人受到伤害 | +80% |

> **满载**是刻意的跨系统钩子：把背包负重从纯粹的负担变成可以主动利用的赌注——带着 −20% 命中开打，赢了额外 +30%。

**表现要求**：战斗界面顶部常驻挑战条，实时显示每条的达成/失败状态；某条判定失败的瞬间要立刻划掉，让玩家知道这场不必再为它扭打法。

---

## 六、战利品与背包

### 6.1 探索层不再产出货币

探索层产出的全部是**实物**。城镇通货是**城市居民积分**，主要来自控制终端工单，以及把带回的战利品卖给量子隧穿售货机。

| 物品类别 | 占格 | 用途 |
| --- | ---: | --- |
| 废件 | 1 | 卖给售货机换积分 |
| 模组材料 | 1 | 制造与装配关键词模组 |
| 装备 | 2 | 直接装备或分解 |
| 数据存档 | 1 | 回城解锁叙事 + 积分 |
| 消耗品（营养膏等） | 1 | 探索途中使用 |

### 6.2 负重

小队共用 **32 格**。每占用 1 格，全队命中率、暴击率与闪避率各降低 **1%**。

**产出配额**：首图一趟 6 段远征的物品产出应控制在 **12-18 格**，对应 −12% 到 −18% 的惩罚——能感觉到疼，但还打得动。**首图新增事件必须按这个数字反推，不能随意加产出。**

### 6.3 背包的开放时机

| 阶段 | 可否开背包 | 理由 |
| --- | --- | --- |
| `revealing` 线路展示 | **禁止** | 否则等于无限延长观察时间，直接废掉核心机制 |
| `choosing` 选择入口 | 允许，不限时 | 主要的整理时机 |
| `routing` 走线中 | 禁止 | 保持走线仪式感不被打断 |
| `landed` 落点选分支 | 允许，不限时 | 「带着什么」本身就是选哪条分支的依据 |
| `resolving` 事件结算 | 允许（含背包满时的替换流程） | 刚拿到东西，正是取舍的时候 |
| 战斗中 | 禁止 | 负重在战斗开始时快照 |

> `revealing` 那条是**硬约束**，必须在 store 层拦截，不能只靠 UI 隐藏按钮。

### 6.4 背包面板行为

- 网格化 32 格，按 §6.1 的占格规则排布。
- 丢弃**即时生效**，面板顶部实时显示 `负重 14/32 · 命中 −14% 暴击 −14% 闪避 −14%`，让玩家边丢边看数字回升。
- 丢弃**不可撤销**，需二次确认（丢的可能是稀有装备）。
- **背包满时**自动弹出同一面板并进入「替换模式」：新物品浮在顶部，必须丢够格子才能拿，或点「放弃拾取」。
- 消耗品可在 `choosing` / `landed` / `resolving` 阶段直接使用，**不额外消耗净化粒子**——携带成本已由负重收过一次，不重复收费。

### 6.5 投递口

**传送投递口**是背包玩法的解压阀，也是首图的必备教学点：把背包内选中的物品**提前寄回据点**，安全落袋，不受后续团灭影响。代价为 `E −5`。

没有它，玩家在后段只能在「背着满包打不动」和「丢掉稀有装备」之间二选一，缺少第三条路。

---

## 七、探索指令与角色能力

### 7.1 原则

探索能力不能只做「多看一秒」。它们应让玩家获得信息、保存信息、重构路径或为错误结果投保，从而把阿弥陀签从纯记忆测试变成资源决策。

探索层使用独立的**探索指令**，不直接消耗战斗手牌。探索指令可由上阵角色、关键词模组、特殊事件或商人提供。

推荐结构为：

- 每名角色带一个稳定的探索被动，体现远征身份。
- 角色、模组与事件共同构成有限的探索指令资源。
- 指令在每段生成线路后、确认入口前使用；少数保险类指令可在走线结束但事件尚未结算时使用。

### 7.2 指令样例

| 指令 | 效果 | 策略意义 |
| --- | --- | --- |
| 拓扑扫描 | 本段线路额外显示 1 秒 | 为关键信息付费，避免盲猜 |
| 残影缓存 | 线路隐藏后，保留最近两条横线轮廓 | 降低记忆负担，但不会直接给答案 |
| 信号锚点 | 指定一条横线，隐藏后仍持续可见 | 锁定关键换线点 |
| 并行探针 | 显示两个入口各自抵达的终点 | 缩小目标范围，仍保留判断空间 |
| 路由重接 | 交换两个相邻终点承载的事件 | 改变目标价值，而不破坏线路规则 |
| 紧急回滚 | 走线结束、事件未结算时返回入口重选一次 | 有代价的悔棋，保留已知信息 |
| 静默接入 | 本段基础能量消耗 −5 | 延长远征寿命 |

`紧急回滚` 不应成为无限免费撤销。可采用「消耗稀有指令」「每次远征固定次数」「额外消耗净化粒子」或其组合来限制。

### 7.3 角色探索身份示例

| 角色定位 | 被动 | 专属指令方向 |
| --- | --- | --- |
| 工程型 | 开局永久显示一条横线 | 断开一条横线、重接相邻线路 |
| 侦察型 | 线路展示时间 +1 秒 | 标记横线、并行探针 |
| 黑客型 | 首次路由类事件额外收益 | 交换终点事件、伪造通行令 |
| 医疗/生存型 | 污染层数伤害减免 | 回滚后保留部分资源、净化终点 |
| 重装型 | 可忽略一次强制换线 | 强行穿越但 E −5 |
| 拾荒型 | 负重惩罚减半 | 提高本段掉落品质 |

这使「带谁出门」同时影响战斗和探索，而不是仅比较战斗数值。

---

## 八、首图事件池：废弃楼层

`neon-city / 废弃楼层` —— 大厦内部、控制终端所在层，清运机械按旧指令把活人当废品回收。

**首图的职责是教机制**，所以事件池要窄、结果要一眼看懂。三条主题线：

1. **清运回路**——`scrap-bot` / `radio-bot` 不是猎人而是作业中的机械。
2. **仍在供电的设施**——门禁、货梯、维护终端、消防喷淋、售货机。
3. **上一批苏醒者的遗留**——营地、遗书、私藏物资，承载叙事与数据存档。

首图**不引入**污染泄漏、坠落、生物失控——那是下水管道、天空玻璃轨道与温室花园的性格。

### 8.1 生存类

| 事件 | 效果 | E |
| --- | --- | ---: |
| 员工休息室 | 全队回 25% 最大 HP，即时 | 0 |
| 应急医疗柜 | 单个角色回满，其余回 10% | 0 |
| 消防喷淋室 | 污染层数 −1，队伍 HP +15% | −4 |

### 8.2 成长类

| 事件 | 效果 | E |
| --- | --- | ---: |
| 废品分拣带 | 废件 ×3 | −2 |
| 拆解台 | 模组材料 ×1 | −2 |
| 上批苏醒者营地 | 数据存档 ×1 + 一段信件文本（叙事解锁） | 0 |
| 未清空的储物柜 | 废件 ×1 + 探索指令 ×1 | −2 |

### 8.3 战斗类

| 事件 | 效果 | E |
| --- | --- | ---: |
| 清运班组 | 普通战斗（2 敌） | −4 |
| 巡回信标 | 普通战斗（`radio-bot` 主导） | −4 |
| 报废压缩机（精英，中后段） | 精英战 | −7 |
| 被当作废品 | 战斗，我方首回合手牌 −1；掉落系数额外 ×1.3 | −4 |

### 8.4 经济类

| 事件 | 效果 | E |
| --- | --- | ---: |
| 量子隧穿售货机 | 用居民积分换营养膏与道具 | 0 |
| 流浪回收商（电子生命） | 废件 ↔ 材料 / 指令 互换 | 0 |
| 控制终端·临时工单 | 接一条本段内可完成的小任务 → 居民积分 | −3 |
| 传送投递口 | 把选中物品寄回据点，安全落袋 | −5 |

### 8.5 路由类（带持续段数）

`RouteEvent` 增加 `duration` 字段（单位：段），由事件自己声明。

| 事件 | 效果 | 段数 | E |
| --- | --- | ---: | ---: |
| 维护终端 | 展示时长 +1.0 秒 | 1 | 0 |
| 楼层平面图 | 每段永久显示 2 条横线 | 3 | 0 |
| 门禁主控 | 每段开局显示 1 个入口的终点 | 2 | −2 |
| 信号中继站 | 走线动画放慢 50%，可在落点前使用保险类指令 | 2 | −2 |
| 短接器 | 获得 1 次「紧急回滚」 | 一次性 | −3 |

### 8.6 能量类

| 事件 | 效果 | E |
| --- | --- | ---: |
| 逆流净化机 | 本段掉落全部作废 | **+25** |
| 隐匿通道 | 免除本段的基础消耗 | +10 等效 |
| 强拆配电柜 | 模组材料 ×2 | −8 |
| 通风破口 | 装备 ×1（品质按当前档位） | −12 |

### 8.7 风险类

**纯负面**（每段最多 1）：

| 事件 | 轴 | 效果 | E |
| --- | --- | --- | ---: |
| 警报误触 | 能量 | 被迫绕路 | −12 |
| 破裂的通风管 | 掉血 + 能量 | 全队 −12% HP | −8 |
| 磁场干扰区 | debuff | 下 2 段线路展示时长 −0.3 秒 | 0 |
| 塌落的隔断 | 路由 | 下 1 段随机 2 个入口被封锁（不可选） | 0 |
| 压力门夹层 | 资源 | 强制丢弃背包 4 格物资 | 0 |
| 静电风暴 | debuff | 下 1 场战斗全队起手手牌 −1 | 0 |

**高风险高收益**（负面 + 明确对价）：

| 事件 | 效果 | E |
| --- | --- | ---: |
| 静电放电区 | 全队 −15% HP → 稀有废件 ×2 | 0 |
| 污染储藏间 | 污染层数 +1（本次远征内不可自行清除） → 稀有材料 ×1 | 0 |
| 超载货梯 | 60% 直接跳过下一段（省能量）／ 40% 全队 −20% HP | 0 |

### 8.8 终局类（第 5 段起）

| 事件 | 效果 |
| --- | --- |
| BOSS 接入点 | 进入 BOSS 战，界面预览当前档位下的强度与掉落 |
| 撤离升降机 | 立即结束远征，背包全部带回（能量第 5 档起必现） |
| 最后补给 | 全队回 40% HP + 消耗品 ×2 |
| 高风险宝库 | 装备 ×2（品质按档位），E −15 |

---

## 九、数据与逻辑边界

### 9.1 `src/explore/` 纯逻辑层

探索层保持与战斗引擎平行的纯 TypeScript 模块：无 React、无副作用、可序列化、可复现、可单测。

| 文件 | 职责 |
| --- | --- |
| `src/explore/types.ts` | 路由图、节点事件、探索指令、队伍快照、背包、会话状态等共享类型 |
| `src/explore/rules.ts` | 路由规模、展示时长、段数、**能量档位表**、`K_global`、品质权重、负重惩罚、团灭规则等平衡旋钮 |
| `src/explore/session.ts` | 建局、生成合法线路、展示状态、使用指令、选入口、走线、事件与战斗回填、背包操作、结束判定 |
| `src/explore/loot.ts` | 掉落结算：系数合成、掉落表掷点、稀有度权重、经验缩放 |
| `src/explore/session.test.ts` | 线路合法性、入口终点双射、指令效果、能量档位、掉落系数、战斗回填与远征结束测试 |

### 9.2 核心类型建议

```ts
export type RouteEventKind =
  | "battle"
  | "elite"
  | "boss"
  | "retreat"
  | "loot"
  | "heal"
  | "merchant"
  | "route"
  | "energy"
  | "hazard";

export interface RouteEvent {
  id: string;
  kind: RouteEventKind;
  title: string;
  description: string;
  energyDelta: number;   // 取代旧的 dangerDelta
  duration?: number;     // 路由/debuff 类事件的持续段数
}

export interface RouteCrossbar {
  row: number;
  leftLane: number; // 仅允许 0-3，连接 leftLane 与 leftLane + 1
}

export interface RouteBoard {
  segment: number;
  laneCount: number;
  crossbars: RouteCrossbar[];
  events: RouteEvent[]; // 终点从左到右排列
  revealDurationMs: number;
  blockedLanes: number[]; // 「塌落的隔断」等 debuff 封锁的入口
}

export interface PartySnapshot {
  charId: string;
  hp: number;
  maxHp: number;
  alive: boolean;
}

export interface ItemStack {
  itemId: string;
  count: number;
  slots: number;   // 单件占格
  quality: "common" | "fine" | "rare";
}

export interface RouteModifier {
  id: string;              // 对应事件 id
  remainingSegments: number;
  effect: string;          // 由 rules 解释
}

export interface RouteHistoryEntry {
  segment: number;
  entryLane: number;
  exitLane: number;
  eventId: string;
  energyBefore: number;
  energyAfter: number;
  note: string;
}

export interface ExploreState {
  mapId: string;
  energy: number;          // 净化粒子，唯一难度轴
  taint: number;           // 污染层数
  segment: number;
  segmentCount: number;
  board: RouteBoard | null;
  party: PartySnapshot[];
  backpack: ItemStack[];
  shippedHome: ItemStack[]; // 已通过投递口寄回，团灭不丢
  instructions: ExploreInstruction[];
  routeModifiers: RouteModifier[];
  history: RouteHistoryEntry[];
  pendingEncounterId: string | null;
  pendingIsBoss: boolean;
  pendingChallenges: string[]; // 本场战斗随机附带的挑战词条
  bossAvailable: boolean;
  phase:
    | "revealing"
    | "choosing"
    | "routing"
    | "landed" // 已抵达终点，效果未生效，等玩家在浮层里选分支（见 §2.3.1）
    | "resolving"
    | "inBattle"
    | "cleared"
    | "retreated"
    | "wiped";
  rngState: number;
}
```

### 9.3 线路生成约束

线路生成器必须在生成时满足：

- 横线只连接相邻竖线。
- 同一 `row` 的横线不可重叠或共享端点。
- 每段横线数符合当前难度区间。
- 图形不能出现无意义的完全空白区；同一对线路的重复连接需要受限。
- 使用当前 `rngState`，保证同一存档或测试种子可复现。

入口到终点的映射可以在生成后预先计算，供指令系统与动画系统使用；玩家界面在隐藏状态不得直接读取或泄露该映射。

### 9.4 地图配置建议

旧的 `explorePool`、`encounterCount` 和路线牌配置不再适用：

```ts
export interface MapDef {
  id: string;
  name: string;
  desc: string;
  difficulty: number;
  emoji: string;

  routeSegments: number;       // 建议 5-7
  bossAvailableFrom: number;   // 建议第 5 段
  eventPoolsByStage: string[][];
  bossEncounterId: string;
  fillerEnemyIds: string[];    // 高档位时追加的敌人
  challengePool: string[];     // 本图战斗可随机到的挑战词条
  startingEnergy: number;      // 默认 100
}
```

各地图可通过事件池与线路规则塑造性格：

- 大厦内部房间：维护终端、门禁、补给和低风险战斗较多。
- 下水管道：污染、净化、绕行与伏击较多。
- 天空玻璃轨道：高收益、高坠落风险、短接与高速路线较多。
- 温室花园：采集、治疗、生物失控与隐蔽路线较多。

---

## 十、战斗与状态流转

### 10.1 战斗衔接

终点事件为战斗、精英或 BOSS 时：

1. 当前探索状态保存 `pendingEncounterId`、当前能量档位与队伍快照。
2. **负重在此刻快照**，转为战斗内的命中/暴击/闪避修正。
3. 战斗以探索层的队伍当前 HP 初始化，并应用污染层数。
4. 根据能量档位生成 `EncounterModifier`。
5. 随机 1-2 条挑战词条并公示。
6. 战斗胜利后：结算挑战达成情况 → 合成 `K` → 按 `K` 结算经验与掉落 → 经验即时入账 → 掉落进背包（满则走替换流程）→ 幸存者 HP 回填探索会话。
7. 非 BOSS 胜利后回到该段的事件结算，再生成下一段路由图。
8. BOSS 胜利后通关结算；全队阵亡则背包全部遗失。

### 10.2 状态机

```text
menu -> town -> expedition -> explore.revealing
                                  |
                                  v
                           explore.choosing  <-- 可开背包
                                  |
                                  v
                           explore.routing
                                  |
                                  v
                          explore.landed  <-- 可开背包；浮层里二选一，效果尚未生效
                           |              |
                      非战斗分支        战斗分支
                           v              |
                         explore.resolving  <-- 可开背包
                           |              |
                    确认后推进           battle（挑战词条公示）
                           |              |
                           +----<--- 经验 / 掉落 / HP 回填
                                  |
                    扣能量 -> 下一段 / BOSS / 撤退 / 团灭
                                  |
                                  v
                               end -> town
```

---

## 十一、界面与表现

### 11.1 主界面信息层级

```text
地图名 | 当前段数 3/6 | 净化粒子能量条(5 档) | 污染层数 | 负重 14/32
队伍血量、异常状态
路由状态栏：生效中的 buff/debuff 图标 + 剩余段数

终点事件 1-5：始终可见
阿弥陀签线路：短暂全显 -> 隐去 -> 实际走线时逐段点亮
入口 A-E：隐藏后始终可选（被封锁的入口置灰）

探索指令栏：扫描 / 锚定 / 探针 / 重接 / 回滚
背包按钮（仅 choosing / landed / resolving 阶段可点）
本次远征记录：已走段数、落点事件、能量变化
```

### 11.2 关键反馈

- 线路显示阶段应有短促的「系统同步」表现，并明确倒计时，防止玩家不知何时开始观察。
- 隐藏阶段不应展示任何会直接泄露入口终点映射的高亮或动画。
- 选定入口后，信号必须以足够慢的速度逐个经过横线，最后才显示落点结果；这是每段的核心仪式感。
- **入口 A-E 是全屏最重要的交互点，就该是全屏最显眼的物件**：当前实现为「悬挂灯箱」（吊索 + 切角箱体 + 底沿灯管），常态缓慢摆动、悬停停摆下压并把所属竖线整条点亮、封锁态断一根吊索歪挂。
- ⛔ **禁止任何闪烁型画面变化**（`infinite/alternate` 的明暗关键帧、高频脉冲）。常驻动效只用匀速或缓入缓出的位移/旋转；明暗一律走一次性的 transition 或播完即止的动画。
- 落点浮层打开时**不加全屏遮罩**——压暗会挡住刚刚亮起来的路径，而那正是玩家此刻最想看的。聚焦改由「四角 HUD 轻度后退 + 其余四张终点卡压暗 + 落点卡升起一道光柱指向面板」承担。
- 使用指令时，应清晰标明改变的是「显示时间」「可见横线」「终点事件」还是「回滚机会」。
- **能量跨档、污染层数增加、BOSS 接入、撤离出口出现需有强提示**——这是玩家判断「还能不能再走一段」的全部依据。
- **路由状态栏**的 buff/debuff 到期前一段要预警。
- 战斗界面顶部常驻**挑战条**，实时显示达成/失败；失败瞬间划掉。
- 远征结算页展示**背包清单**而非一个数字：列出带回的每件物品；团灭时整页划掉，寄回的部分单独保留。

### 11.3 可访问性与难度

短时记忆是特色，但不能成为不可跨越的门槛：

- 默认展示时长不低于 0.8 秒。
- 提供基础的重看机会或可获得的扫描指令。
- 可在设置中提供更长展示时间的辅助模式；对应的分数、成就或额外奖励规则如需区分，应单独设计，不能静默惩罚。
- 线路布局要避免纯视觉噪声：横线、入口、终点和当前走线路径必须有清晰层级与高对比度。

---

## 十二、实施范围

### P0：最小可玩闭环

1. 实现可复现的 5 线阿弥陀签生成、入口到终点映射与走线动画数据。
2. 实现 5 段固定地图流程：线路展示、选择入口、结算基础事件。
3. 接入普通战斗、HP 跨段继承、**净化粒子档位**与主动撤退。
4. **删除危险度与残片**：`DANGER_TIERS` → `ENERGY_TIERS`，`shards` → 实物背包 + 居民积分。
5. 在第 5 段后接入 BOSS 事件，打通 BOSS 胜利、撤退与团灭结算。
6. 完成纯逻辑单测：线路合法性、映射、种子复现、事件生成保底、能量档位、战斗回填。

### P1：掉落、背包与手感

1. 敌人掉落表 + 统一掉落系数 `K` + 稀有度权重。
2. 背包面板：网格、负重实时反馈、丢弃、替换模式、投递口。
3. 挑战词条：随机附带、战斗内实时判定、战斗条 UI。
4. `拓扑扫描`、`信号锚点`、`并行探针`、`紧急回滚` 四种基础指令。
5. 线路显示、隐藏、走线、落点和跨档的完整视觉反馈；路由状态栏。

### P2：角色与内容深度

1. 角色探索被动与关键词模组的探索侧能力。
2. `路由重接`、`静默接入` 等主动干预指令。
3. 商人、量子售货机、工单事件和地图专属机制。
4. 5-7 段不同长度、不同线路难度的地图配置与各自的挑战词条池。

---

## 十三、待后续验证的平衡问题

1. **展示时间与横线数的关系**：优先保证「记住关键换线」可行，而非追求看不清的压迫感。
2. **重看资源的供给量**：太多会使线路图失去意义，太少则会将玩法变成记忆门槛。
3. **事件保底与随机性的比例**：每段要有可行出路，但不能让玩家每次都能稳定找到最优解。
4. **能量消耗速度**：首图必须让玩家撞到时限，但不能在第 4 段就逼到枯竭。
5. **BOSS 接入时机**：太早会压缩探索，太晚会让撤退缺少张力；首张地图从第 5 段接入是合适起点。
6. **产出配额与负重惩罚**：12-18 格是首图的目标区间。若实测下来玩家总是满包，先加投递口的出现率，再考虑放宽 32 格。
7. **经验也吃掉落系数**的长期影响：这会让保守玩法在养成上明显落后。若差距过大，考虑给经验单独设一个更平缓的系数。
8. **挑战词条的加成幅度**：单场上限 +150% 是初值。「无伤 +80%」若被证明太容易白嫖，优先下调它而不是整体压缩。
9. **消耗品免费使用**：当前设定为不额外消耗能量（携带成本已由负重收过）。若测下来营养膏让 HP 压力消失，再考虑加使用代价。

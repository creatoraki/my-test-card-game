# 探索模式设计：失真路由图

> 状态：**设计定稿，尚未按本方案实现。**
>
> 本文以当前确认的「阿弥陀签式路由决策」替换旧版探索事件卡牌局方案。旧方案中的事件卡牌库、抽牌权、路线牌与横向轨迹不再保留。

---

## 一、设计目标

### 1.1 核心体验

玩家接入一座长期失修、不断重排的城市维护网络。每次接入前，系统会短暂展示一张由入口、线路和终点构成的失真路由图；玩家选择一个入口后，信号会沿线路自动下行，遇到横线时必须切换至相邻线路，最终抵达一个事件终点。

玩家知道每个终点是什么事件，却不能在隐藏线路后完全确定入口会通向哪里。核心问题是：

> 在这次远征状态下，我最需要抵达哪个事件？我是否记住了通向它的线路？要不要消耗有限的探索指令来确认或改写结果？

玩法的紧张感来自短时信息、远征资源和角色能力的共同管理，而非纯粹拼手速或视觉记忆。

### 1.2 一次远征的结构

一张地图由 **5 至 7 段独立的路由决策关卡**构成。每段走完一次完整的 `A-E -> 1-5` 路线后，只结算一个事件；随后生成下一段全新的入口、线路与事件终点。

```text
生成本段 5 个终点事件
        ↓
完整线路显示约 1 秒
        ↓
线路隐去，玩家选择入口 A-E
        ↓
信号自动沿线路下行，遇横线强制换线
        ↓
抵达 1-5 的某个事件并结算
        ↓
未结束：生成下一段路由图
结束：BOSS / 撤退 / 远征结算
```

标准地图建议为 6 段；短地图为 5 段，深层或高难地图可扩展至 7 段。

### 1.3 保留的远征张力

旧方案中以下系统仍然成立，并构成新玩法的全局层：

- **队伍 HP 跨战斗继承**：医疗、避战、撤退与补给终点才有真实价值。
- **区域危险度**：玩家在事件中制造的动静会提高危险度，进而提高后续战斗、精英和 BOSS 的难度与奖励。
- **BOSS 与撤退并存**：中后段接入 BOSS 路由，玩家可选择挑战、撤离，或继续赌一次高价值事件。
- **残片暂存**：战斗经验即时结算；残片在撤退或通关时落袋，团灭时按规则损失。

---

## 二、单段路由规则

### 2.1 图形结构

基础路由图使用 5 条竖线、5 个入口和 5 个终点：

```text
入口：    A     B     C     D     E
          |     |-----|     |     |
          |-----|     |     |-----|
          |     |     |-----|     |
          |     |-----|     |     |
终点：    1     2     3     4     5
```

- 信号从所选入口自上而下移动。
- 遇到横线时，信号必须横移到相邻竖线后继续向下。
- 一张图的线路必须是合法阿弥陀签：横线仅连接相邻竖线，且同一高度不得共享端点。
- 每个入口必定且仅会抵达一个终点；每个终点也恰好对应一个入口。
- 默认所有终点事件可见；中间线路在展示结束后隐去，入口标签和终点事件继续可见。

### 2.2 观察与选择

1. 系统生成 5 个终点事件和一张对应线路图。
2. 完整线路默认显示 **1.0 秒**，显示期间不可选择。
3. 线路隐藏后，玩家不限时选择 A-E 的任意入口。
4. 玩家确认后，信号沿真实线路移动；移动动画逐段显示实际经过的竖线与横线。
5. 抵达终点后，结算该事件，记录本段结果，并进入下一段或结束远征。

基础展示时长随地图和进度调整，但不应将难度简单堆到极短时间：

| 阶段 | 横线数建议 | 默认展示时长 |
| --- | ---: | ---: |
| 前段 | 4-6 | 1.2 秒 |
| 中段 | 6-9 | 1.0 秒 |
| 后段 | 9-12 | 0.8-1.0 秒 |

### 2.3 事件结算

一次走线只触发一个终点事件。事件可能立即结算，也可能进入战斗、商店或特殊交互；结算完成才生成下一段线路。

建议每段的五个终点至少覆盖不同决策维度，而不是简单地排列五档数值奖励：

| 类别 | 例子 | 主要价值 |
| --- | --- | --- |
| 生存 | 医疗站、净化站、护盾修复 | 降低后续团灭风险 |
| 成长 | 残片、模组材料、经验 | 提高长期收益 |
| 战斗 | 普通战斗、精英战、伏击 | 以风险换经验与稀有收益 |
| 经济 | 商人、量子售货机、交换站 | 将既有资源转换为所需资源 |
| 路由 | 情报节点、稳定器、短接器 | 改变后续线路的信息或操作优势 |
| 风险 | 污染泄漏、巡逻锁定、故障门 | 负面结果，或高风险高收益 |

事件生成保底规则：

- 每段至少 1 个生存或低风险终点。
- 每段至少 1 个成长终点。
- 每段至少 1 个战斗或高风险终点。
- 同一段最多 1 个纯负面事件。
- 精英、BOSS、高额资源和强制危险度变化仅在中后段出现。

---

## 三、远征节奏与终局

### 3.1 推荐的六段地图

| 段数 | 设计职责 | 事件倾向 |
| --- | --- | --- |
| 第 1 段 | 建立方向 | 基础资源、轻度战斗、少量补给 |
| 第 2 段 | 首次分流 | 商人、普通战斗、路线资源 |
| 第 3 段 | 风险抬升 | 精英、高收益、陷阱、恢复 |
| 第 4 段 | 中局压力 | 战斗、危险度控制、强资源 |
| 第 5 段 | 收束选择 | BOSS 接入、撤离、补给、宝库 |
| 第 6 段 | 终局 | BOSS、撤退或最终高风险奖励 |

不要将最后一段机械固定为 BOSS。建议在第 4 或第 5 段后让 BOSS 接入网络，使后续终点池可能出现：

- BOSS 接入点；
- 撤离升降机；
- 最后补给；
- 高风险宝库；
- 隐匿路线。

这样玩家能够带着已获得的残片撤离、立刻挑战 BOSS，或为了更好的状态与收益再进行一段路由决策。

### 3.2 远征结束方式

| 结束方式 | 残片 | 经验 | 说明 |
| --- | --- | --- | --- |
| BOSS 胜利 | 全部落袋 + BOSS 掉落 | 已即时入账 | 通关 |
| 主动撤退 | 全部落袋 | 已即时入账 | 不算失败 |
| 团灭 | 按团灭规则损失 | 已即时入账 | 角色回城镇复原 |

经验与残片刻意不对称：每场战斗的经验即时结算，而残片只会在撤退或通关时进入城镇。撤退因此是有张力的主动选择，而不是失败按钮。

---

## 四、区域危险度

### 4.1 职责

路由图负责决定“本段会落到什么事件”；危险度负责描述“整趟远征已经惊动城市到什么程度”。两者不得合并为同一套数值。

危险度主要由事件结算改变，而不是由选择入口本身改变：

- 普通探索、资源回收、常规战斗：通常 `+1`。
- 强制开门、破坏设施、精英和高收益节点：通常 `+2` 或更多。
- 隐匿出口、清理监控、特定角色能力：可降低或避免提升。
- 回溯线路不应自动降低危险度，城市已记录到此前的入侵痕迹。

### 4.2 档位

数值范围为 0-15，每 3 点一档；第 5 档后数值可继续累积但档位封顶。

| 档位 | 名称 | 数值 | 战斗与奖励影响 |
| --- | --- | --- | --- |
| 1 | 平静 | 0-2 | 基础敌人，倍率 x1.0 |
| 2 | 骚动 | 3-5 | 敌人力量 +1，倍率 x1.3 |
| 3 | 警戒 | 6-8 | 追加 1 名敌人，倍率 x1.6 |
| 4 | 危险 | 9-11 | 追加敌人、力量 +2、护盾 +5、行动更快，倍率 x2.0 |
| 5 | 猩红 | 12+ | 追加 2 名敌人、力量 +3、护盾 +8、行动更快，倍率 x2.6 |

### 4.3 BOSS 缩放

BOSS 以开打时危险度档位作为额外缩放依据：

- BOSS `maxHp` 乘以 $1 + 0.15 \times (档位 - 1)$。
- BOSS 掉落乘以 $1 + 0.4 \times (档位 - 1)$。
- 第 4 档起追加 1 名护卫。

BOSS 接入点出现后，界面应实时预览当前档位下的 BOSS 强度与掉落，帮助玩家决定现在挑战还是继续探索。

---

## 五、探索指令与角色能力

### 5.1 原则

探索能力不能只做“多看一秒”。它们应让玩家获得信息、保存信息、重构路径或为错误结果投保，从而把阿弥陀签从纯记忆测试变成资源决策。

探索层使用独立的**探索指令**，不直接消耗战斗手牌。探索指令可由上阵角色、关键词模组、特殊事件或商人提供。

推荐结构为：

- 每名角色带一个稳定的探索被动，体现远征身份。
- 角色、模组与事件共同构成有限的探索指令资源。
- 指令在每段生成线路后、确认入口前使用；少数保险类指令可在走线结束但事件尚未结算时使用。

### 5.2 指令样例

| 指令 | 效果 | 策略意义 |
| --- | --- | --- |
| 拓扑扫描 | 本段线路额外显示 1 秒 | 为关键信息付费，避免盲猜 |
| 残影缓存 | 线路隐藏后，保留最近两条横线轮廓 | 降低记忆负担，但不会直接给答案 |
| 信号锚点 | 指定一条横线，隐藏后仍持续可见 | 锁定关键换线点 |
| 并行探针 | 显示两个入口各自抵达的终点 | 缩小目标范围，仍保留判断空间 |
| 路由重接 | 交换两个相邻终点承载的事件 | 改变目标价值，而不破坏线路规则 |
| 紧急回滚 | 走线结束、事件未结算时返回入口重选一次 | 有代价的悔棋，保留已知信息 |
| 静默接入 | 本段触发战斗时危险度少增 1 | 降低高价值战斗的总成本 |

`紧急回滚` 不应成为无限免费撤销。可采用“消耗稀有指令”“每次远征固定次数”“提高危险度”或其组合来限制。

### 5.3 角色探索身份示例

| 角色定位 | 被动 | 专属指令方向 |
| --- | --- | --- |
| 工程型 | 开局永久显示一条横线 | 断开一条横线、重接相邻线路 |
| 侦察型 | 线路展示时间 +1 秒 | 标记横线、并行探针 |
| 黑客型 | 首次路由类事件额外收益 | 交换终点事件、伪造通行令 |
| 医疗/生存型 | 陷阱与污染伤害减免 | 回滚后保留部分资源、净化终点 |
| 重装型 | 可忽略一次强制换线 | 强行穿越但危险度 +1 |

这使“带谁出门”同时影响战斗和探索，而不是仅比较战斗数值。

---

## 六、数据与逻辑边界

### 6.1 `src/explore/` 纯逻辑层

探索层保持与战斗引擎平行的纯 TypeScript 模块：无 React、无副作用、可序列化、可复现、可单测。

建议模块职责：

| 文件 | 职责 |
| --- | --- |
| `src/explore/types.ts` | 路由图、节点事件、探索指令、队伍快照、会话状态等共享类型 |
| `src/explore/rules.ts` | 路由规模、展示时长、段数、危险度档位、团灭规则等平衡旋钮 |
| `src/explore/session.ts` | 建局、生成合法线路、展示状态、使用指令、选入口、走线、事件与战斗回填、结束判定 |
| `src/explore/session.test.ts` | 线路合法性、入口终点双射、指令效果、危险度、战斗回填与远征结束测试 |

### 6.2 核心类型建议

```ts
export type RouteEventKind =
  | "battle"
  | "elite"
  | "boss"
  | "retreat"
  | "loot"
  | "heal"
  | "merchant"
  | "route"
  | "hazard";

export interface RouteEvent {
  id: string;
  kind: RouteEventKind;
  title: string;
  description: string;
  dangerDelta: number;
}

export interface RouteCrossbar {
  row: number;
  leftLane: number; // 仅允许 0-3，连接 leftLane 与 leftLane + 1
}

export interface RouteBoard {
  segment: number;
  laneCount: number;
  crossbars: RouteCrossbar[];
  events: RouteEvent[]; // 终点从左到右排列
  revealDurationMs: number;
}

export interface PartySnapshot {
  charId: string;
  hp: number;
  maxHp: number;
  alive: boolean;
}

export interface RouteHistoryEntry {
  segment: number;
  entryLane: number;
  exitLane: number;
  eventId: string;
  dangerBefore: number;
  dangerAfter: number;
  note: string;
}

export interface ExploreState {
  mapId: string;
  danger: number;
  loot: number;
  segment: number;
  segmentCount: number;
  board: RouteBoard | null;
  party: PartySnapshot[];
  instructions: ExploreInstruction[];
  history: RouteHistoryEntry[];
  pendingEncounterId: string | null;
  pendingIsBoss: boolean;
  bossAvailable: boolean;
  phase: "revealing" | "choosing" | "routing" | "resolving" | "inBattle" | "cleared" | "retreated" | "wiped";
  rngState: number;
}
```

### 6.3 线路生成约束

线路生成器必须在生成时满足：

- 横线只连接相邻竖线。
- 同一 `row` 的横线不可重叠或共享端点。
- 每段横线数符合当前难度区间。
- 图形不能出现无意义的完全空白区；同一对线路的重复连接需要受限。
- 使用当前 `rngState`，保证同一存档或测试种子可复现。

入口到终点的映射可以在生成后预先计算，供指令系统与动画系统使用；玩家界面在隐藏状态不得直接读取或泄露该映射。

### 6.4 地图配置建议

旧的 `explorePool`、`encounterCount` 和路线牌配置不再适用。地图改为定义路由段数、各段事件池与 BOSS 接入时间：

```ts
export interface MapDef {
  id: string;
  name: string;
  desc: string;
  difficulty: number;
  emoji: string;

  routeSegments: number;       // 建议 5-7
  bossAvailableFrom: number;   // 建议第 5 段
  eventPoolsByStage: string[][];
  bossEncounterId: string;
  fillerEnemyIds: string[];
}
```

各地图可通过事件池与线路规则塑造性格：

- 大厦内部房间：维护终端、门禁、补给和低风险战斗较多。
- 下水管道：污染、净化、绕行与伏击较多。
- 天空玻璃轨道：高收益、高坠落风险、短接与高速路线较多。
- 温室花园：采集、治疗、生物失控与隐蔽路线较多。

---

## 七、战斗与状态流转

### 7.1 战斗衔接

终点事件为战斗、精英或 BOSS 时：

1. 当前探索状态保存 `pendingEncounterId`、当前危险度与队伍快照。
2. 战斗以探索层的队伍当前 HP 初始化。
3. 根据危险度档位生成 `EncounterModifier`。
4. 战斗胜利后，经验即时入账，幸存者 HP 回填探索会话。
5. 非 BOSS 胜利后回到该段的事件结算，再生成下一段路由图。
6. BOSS 胜利后通关结算；全队阵亡则按团灭规则结束。

### 7.2 状态机

```text
menu -> town -> expedition -> explore.revealing
                                  |
                                  v
                           explore.choosing
                                  |
                                  v
                           explore.routing
                                  |
                                  v
                         explore.resolving
                           |              |
                    普通事件结算        战斗终点
                           |              v
                           |           battle
                           |              |
                           +----<--- reward / HP 回填
                                  |
                    下一段 / BOSS / 撤退 / 团灭
                                  |
                                  v
                               end -> town
```

---

## 八、界面与表现

### 8.1 主界面信息层级

```text
地图名 | 当前段数 3/6 | 危险度仪表 | 暂存残片
队伍血量、异常状态、过滤装置能量

终点事件 1-5：始终可见
阿弥陀签线路：短暂全显 -> 隐去 -> 实际走线时逐段点亮
入口 A-E：隐藏后始终可选

探索指令栏：扫描 / 锚定 / 探针 / 重接 / 回滚
本次远征记录：已走段数、落点事件、危险度变化
```

### 8.2 关键反馈

- 线路显示阶段应有短促的“系统同步”表现，并明确倒计时，防止玩家不知何时开始观察。
- 隐藏阶段不应展示任何会直接泄露入口终点映射的高亮或动画。
- 选定入口后，信号必须以足够慢的速度逐个经过横线，最后才显示落点结果；这是每段的核心仪式感。
- 使用指令时，应清晰标明改变的是“显示时间”“可见横线”“终点事件”还是“回滚机会”。
- 危险度跨档、BOSS 接入、撤离出口出现需有强提示，避免玩家错过远征的关键状态变化。

### 8.3 可访问性与难度

短时记忆是特色，但不能成为不可跨越的门槛：

- 默认展示时长不低于 0.8 秒。
- 提供基础的重看机会或可获得的扫描指令。
- 可在设置中提供更长展示时间的辅助模式；对应的分数、成就或额外奖励规则如需区分，应单独设计，不能静默惩罚。
- 线路布局要避免纯视觉噪声：横线、入口、终点和当前走线路径必须有清晰层级与高对比度。

---

## 九、实施范围

### P0：最小可玩闭环

1. 实现可复现的 5 线阿弥陀签生成、入口到终点映射与走线动画数据。
2. 实现 5 段固定地图流程：线路展示、选择入口、结算 5 类基础事件。
3. 接入普通战斗、HP 跨战斗继承、危险度档位和主动撤退。
4. 在第 5 段后接入 BOSS 事件，打通 BOSS 胜利、撤退与团灭结算。
5. 完成纯逻辑单测：线路合法性、映射、种子复现、事件生成保底、战斗回填。

### P1：探索指令与手感

1. `拓扑扫描`、`信号锚点`、`并行探针`、`紧急回滚` 四种基础指令。
2. 线路显示、隐藏、走线、落点和跨档的完整视觉反馈。
3. 探索记录与 BOSS 强度实时预览。
4. 各地图不同的路由图视觉主题与事件池。

### P2：角色与内容深度

1. 角色探索被动与关键词模组的探索侧能力。
2. `路由重接`、`静默接入` 等主动干预指令。
3. 商人、量子售货机、路线事件和地图专属机制。
4. 5-7 段不同长度、不同线路难度的地图配置。

---

## 十、待后续验证的平衡问题

1. **展示时间与横线数的关系**：优先保证“记住关键换线”可行，而非追求看不清的压迫感。
2. **重看资源的供给量**：太多会使线路图失去意义，太少则会将玩法变成记忆门槛。
3. **事件保底与随机性的比例**：每段要有可行出路，但不能让玩家每次都能稳定找到最优解。
4. **危险度的增速**：需要让高风险终点值得争取，同时确保保守路线也不会完全失去成长。
5. **BOSS 接入时机**：太早会压缩探索，太晚会让撤退缺少张力；首张地图从第 5 段接入是合适起点。
6. **过滤装置能量与危险度的关系**：过滤装置应作为世界观上的远征时限和撤退压力，不应再复制一条与危险度完全相同的难度曲线。
# 探索卡牌模式 · 设计文档

> 状态：**P0 已实现并跑通**（`explore/` 纯逻辑层 + 21 条单测 + `ExploreScreen` 全套界面）。P1/P2 见第十节。
> 目标：把「进入地图后直接连打 N 场战斗」替换成一个**探索牌局** —— 玩家在牌局里决定这趟远征的节奏、风险与收益，战斗是牌局的一部分而不是全部。

---

## 一、设计目标与核心循环

### 1.1 为什么是牌局，而不是地图

这个游戏的交互语言只有一种：**出牌**。探索层如果做成节点图或房间网格，等于引入第二套语言，学习成本与美术成本都要翻倍。做成牌局则：

- 玩家零学习成本
- 复用现有的 `HandCard` / `CardView` / 出牌动画 / 卡牌详情浮窗
- 「打出的卡横向排开」天然形成一条**探索轨迹**，地图不用画，它自己长出来

### 1.2 核心循环（发动机）

> **牌是资源，而抽牌权只能靠打仗换。**

```
手上有事件卡  ──打出──▶  收益入袋 + 区域危险度↑
     ▲                          │
     │                     手牌打空
     │                          ▼
  抽 2 张  ◀──胜利──  打出「遭遇」卡，进入战斗
                        （难度与奖励按当前危险度）
```

玩家每一手真正在想的只有一句：

> **「这张事件卡，值不值得我为它把危险度再抬一档？」**

压力是结构自带的 —— 不需要口粮、火把、回合限制等任何外挂计时器。

### 1.3 危险度是 BOSS 的燃料

> 「你在这片区域搞出的所有动静，它全听见了。」

BOSS 的强度与掉落，**就是玩家这趟远征累积出来的危险度**。这一条同时解决三个问题：

1. 玩家在用**一整趟的行为**给自己设定难度，而不是开局点一个"困难模式"按钮
2. 危险度单调上升不再是缺陷 —— 那条曲线**正好就是高潮曲线**
3. 「苟着速通」不需要专门惩罚：危险度压得低 → BOSS 瘦 → 打完没变强 → 下张地图吃不消。玩家自己会算明白

BOSS 卡入手后，界面上实时显示它当前的强度与掉落预览，**随玩家继续刷事件卡而变化**。玩家会盯着那张卡犹豫 —— 这就是我们要的。

---

## 二、名词表

| 名词 | 含义 |
| --- | --- |
| **探索牌局 / Session** | 一次远征的完整牌局，从进入地图到通关或撤退 |
| **探索卡** | 牌局里打出的卡，与战斗卡是两套完全独立的体系 |
| **路线牌** | 固定牌：3×〔遭遇〕、1×〔BOSS〕、1×〔撤退〕。不进牌库、不可弃、不占手牌上限 |
| **事件卡** | 进牌库、靠抽取获得的卡（采集 / 商人 / 遗迹 / 陷阱…） |
| **轨迹 / Trail** | 已打出的卡，横向排开保留在牌桌上，构成这趟远征的可视历史 |
| **区域危险度** | 0~15 的整数，分 5 档，决定所有战斗的难度与奖励 |
| **场地能力** | 探索界面上三个常驻按钮，永远可用，用于防止牌局卡死 |
| **战利品 / 残片** | 探索层产出的通用资源，仅在**撤退或通关**时结算进城镇 |
| **随身卡** | 玩家在城镇里配置、洗进地图牌库的少量自带探索卡（P2 阶段） |

---

## 三、探索牌局规则

### 3.1 开局

1. 玩家在远征界面选定地图 → 进入 `explore` 界面
2. **构建牌库**：`MapDef.explorePool` 的全部条目（可重复登记以表示权重）+ 玩家随身卡，洗匀
3. **路线牌直接入手**（独立于牌库）：
   - 3 张〔遭遇〕—— 卡面显示的敌人组合由 `MapDef.encounterPool` 决定
   - 1 张〔撤退〕—— 全程可用
   - BOSS 卡此时**不在手**，处于「潜伏」状态，在轨迹区右端显示为背面
4. **起手抽 3 张事件卡**
5. 危险度初始为 **0**（第 1 档 · 平静）

### 3.2 行动

牌局**没有回合概念**，玩家自由出牌直到牌局结束。任何时刻可以：

| 动作 | 结果 |
| --- | --- |
| 打出**事件卡** | 立即结算效果 → 危险度按卡面变化（默认 +1）→ 卡进入轨迹（**不洗回牌库**） |
| 打出**遭遇卡** | 进入战斗（难度按当前危险度档位）→ 胜利后回到探索并**抽 2 张** |
| 打出 **BOSS 卡** | 进入最终战 → 胜利即通关结算 |
| 打出**撤退卡** | 立即结束远征，已有战利品落袋 |
| 使用**场地能力** | 见 3.5 |

### 3.3 BOSS 的入手

3 张遭遇卡**全部打完**后，BOSS 卡自动加入手牌，轨迹区右端的背面卡翻开。

此后玩家仍可继续刷事件卡抬高危险度（把 BOSS 喂肥），或者立刻开打，或者带着战利品撤退 —— 撤退时 BOSS 不算通关，但战利品保留。

> **手牌里同时躺着〔BOSS〕和〔撤退〕这两张牌**，那种拉扯是自然产生的，不需要任何「确定要撤退吗？」弹窗。

### 3.4 手牌与牌库

- **手牌上限 7**，只约束事件卡；路线牌不占位（UI 上分两段显示）
- 抽牌时手牌已满 → **抽满即止**，剩余抽牌数作废（不弃牌、不让玩家做无意义的取舍）
- **探索卡打出后不洗回牌库**（进轨迹）⇒ 牌库是**有限**的

> ⭐ 这条带来一个漂亮的旋钮：**地图的探索卡池大小 = 这张地图最多能玩多久**。
> 建议首张地图池 14 张：起手 3 + 三场战斗各 2 = 常规能摸到 9 张，剩下 5 张必须靠「搜寻」（危险度 +1）才挖得到底。想要更长更肥的远征？把池子做大。

- 牌库抽空后，抽牌类效果与「搜寻」失效，玩家只剩战斗 / BOSS / 撤退可选

### 3.5 场地能力（三个常驻按钮）

作用是**防止牌局卡死**，同时构成一组小决策 —— 三个按钮各自把一种东西换成另一种：

| 按钮 | 代价 | 收益 | 用于 |
| --- | --- | --- | --- |
| **搜寻** | 危险度 **+1** | 抽 1 张 | 缺牌 |
| **休整** | 弃 **2** 张事件卡 | 全队回复 **25%** 最大 HP | 缺血 |
| **隐匿** | 弃 **2** 张事件卡 | 危险度 **-1** | 危险度冲太高，想在跨档前压回来 |

三个按钮**永远可用**（代价不足时置灰）。牌库空 + 手牌空的死局因此不存在。

### 3.6 队伍血量跨战斗继承 —— **必需项**

探索层的连续性完全依赖这一条：**战斗结束后队伍 HP 不回满，带入下一场**。

没有它，「休整」按钮毫无意义、「还打不打得动下一场」的判断不存在、撤退也不会有人考虑 —— 整套设计会退化成一个好看的过场。

- 战斗结束时把每个存活单位的 `hp` 写回探索会话
- 下一场战斗以该 HP 开局（`AllyInit` 新增 `startHp`）
- 阵亡角色在本次远征内**保持阵亡**（无法出牌），撤退或通关后于城镇复原
- 全队阵亡 = 团灭，见 3.7

### 3.7 牌局结束

| 结束方式 | 战利品（残片） | 经验 | 说明 |
| --- | --- | --- | --- |
| **BOSS 胜利** | 全部落袋 + BOSS 掉落 | 已入账 | 通关 |
| **主动撤退** | 全部落袋 | 已入账 | 随时可用，不算失败 |
| **团灭** | **全部丢失** | 已入账 | 角色不死，回城镇复原 |

> **⚠ 经验与残片刻意不对称**（实现时的一处修正）：经验在**每场战斗结束时即时入账**（按危险度倍率），残片相反——只在**撤退或通关**时才转进城镇。
>
> 这么设计有两个理由：一是保留了现成的战后小结页与 `townStore.grantExp` 流程，不必再做一套「暂存经验」；二是更重要的——**这个不对称正是「撤退」成为真选择的唯一原因**。打赢的经验谁也拿不走，但你兜里的残片随时可能因为贪最后一张牌而全丢。如果两样都会丢，惩罚过重；如果两样都不丢，撤退就没有张力了。
>
> 团灭的惩罚力度是**这套循环里唯一可能需要反复调的旋钮**，写在 `EXPLORE_RULES.wipe.lootKept`（当前 `0`）。后续要加重就往上加（比如追加「重伤」状态需要城镇设施治疗）。

---

## 四、探索卡型

### 4.1 路线牌（固定，不进牌库）

| 卡 | 危险度 | 效果 |
| --- | --- | --- |
| 〔遭遇〕×3 | 0 | 进入一场小怪战斗；胜利后抽 2 张 |
| 〔BOSS〕 | 0 | 进入最终战；3 张遭遇卡打完后自动入手 |
| 〔撤退〕 | 0 | 结束远征，战利品落袋 |

### 4.2 事件卡（首批 14 张，"迷雾森林"卡池示例）

危险度默认 **+1**，特别标注的除外。

| 名称 | 危险度 | 效果 | 说明 |
| --- | --- | --- | --- |
| 采集苔藓 | +1 | 残片 +8 | 最基础的收益卡 |
| 拾荒 | +1 | 残片 +12 | |
| 废弃补给箱 | +1 | 残片 +20 | 稀有 |
| 隐蔽小径 | **0** | 抽 1 张 | 白嫖一张，不涨危险度 |
| 顺风而行 | **0** | 抽 2 张 | 稀有 |
| 旅行商人 | +1 | 打开商店：残片换取消耗品 / 属性点 / 探索卡 | 需子界面，见 4.4 |
| 废墟营地 | +1 | 全队回复 30% 最大 HP | |
| 净水源 | **0** | 全队回复 15% 最大 HP | |
| 惊起鸟群 | **+2** | 残片 +25 | 高收益高代价 |
| 焚烧巢穴 | **+3** | 残片 +40，抽 2 张 | 一张牌跳一整档 |
| 掩埋痕迹 | **-2** | 无 | 纯降危险度，是玩家的刹车 |
| 绕道而行 | **-1** | 弃 1 张 | 便宜的刹车 |
| 陷阱 | +1 | 全队受到 8 点无视护盾伤害 | 负面卡，占牌库位 |
| 迷路 | +1 | 弃 2 张 | 负面卡 |

> 卡池刻意放了 2 张负面卡：牌库有杂质，「搜寻」才是有风险的动作，「抽 2 张」才有分量。

### 4.3 卡池设计准则（后续扩充照此写）

1. **每张卡必须同时动两个数**（收益 + 危险度），只给收益不涨危险度的卡是设计失误
2. **降危险度的卡要有明确代价**（弃牌 / 放弃收益），否则玩家会攒着它无限刷
3. **地图性格靠卡池表达**：森林多采集与回血，城市多商人与情报，深渊多高危高收益 + 更多杂质
4. **稀有度不做抽牌权重**，靠在 `explorePool` 里重复登记来加权（与 `poolCardIds` 同惯例）

### 4.4 商店（P2）

第一期把「旅行商人」简化为 `ADD_CARD` —— 消耗残片直接获得一张随机战斗卡。完整商店界面留到 P2。

---

## 五、区域危险度

### 5.1 档位表

数值范围 0~15，**每 3 点一档**（事件卡默认 +1 ⇒ 打 3 张跳一档）。

| 档 | 名称 | 数值 | 配色 |
| --- | --- | --- | --- |
| 1 | **平静** | 0–2 | 青灰 |
| 2 | **骚动** | 3–5 | 黄 |
| 3 | **警戒** | 6–8 | 橙 |
| 4 | **危险** | 9–11 | 红 |
| 5 | **猩红** | 12+ | 深红（数值继续累加但不再升档） |

> **分档而非连续数值**，是因为决策发生在**跨档的那一手**。玩家看到「再打一张就跳档」会真的停下来算一算；连续数值只会让人麻木地点下去。
> UI 必须明确提示「此牌将使危险度跨入〔警戒〕」。

### 5.2 档位对战斗的四项影响

| 档 | 敌人数量 | 敌人开局状态 | castTick | 战利品 / 经验倍率 |
| --- | --- | --- | --- | --- |
| 1 平静 | 基础 | — | +0 | ×1.0 |
| 2 骚动 | 基础 | 力量 +1 | +0 | ×1.3 |
| 3 警戒 | **+1** | 力量 +1 | +0 | ×1.6 |
| 4 危险 | **+1** | 力量 +2、护盾 +5 | **-1** | ×2.0 |
| 5 猩红 | **+2** | 力量 +3、护盾 +8 | **-1** | ×2.6 |

- 追加的敌人从 `MapDef.fillerEnemyIds` 随机取，站位走默认排布（不覆盖手工站位）
- `castTick` 下限钳制为 **1**
- 倍率同时作用于战利品与经验 —— 难度上去了回报必须跟上，否则没人愿意主动抬危险度

### 5.3 BOSS 的额外缩放

BOSS 战在上表基础上再叠一层，读**开打瞬间**的危险度：

- BOSS 本体 `maxHp` × `1 + 0.15 × (档位 - 1)`
- BOSS 掉落 × `1 + 0.4 × (档位 - 1)`
- 第 4 档起 BOSS 追加一个护卫（`fillerEnemyIds`）

### 5.4 表现层联动

危险度档位下发给 `ambience.ts` / `battleBg.ts`，让玩家**看得见**自己干了什么：

- 探索界面的背景调色随档位偏移（青灰 → 猩红）
- 战斗内 `AmbienceGrade` 的色偏强度随档位提升
- 跨档瞬间播一次全屏警示动画 + 音效

---

## 六、数据结构

### 6.1 `src/data/explore.ts`（新增）

```ts
export type ExploreCardKind = "encounter" | "boss" | "retreat" | "event";

export type ExploreEffect =
  | { type: "GAIN_LOOT"; amount: number }
  | { type: "DRAW"; amount: number }
  | { type: "DISCARD"; amount: number }
  | { type: "HEAL_PARTY"; percent: number }      // 按 maxHp 百分比
  | { type: "DAMAGE_PARTY"; amount: number }     // 无视护盾
  | { type: "GAIN_EXP"; amount: number }
  | { type: "ADD_CARD"; poolOf?: string };       // 给指定角色(缺省随机上阵者)加一张战斗卡

export interface ExploreCardDef {
  id: string;
  name: string;
  kind: ExploreCardKind;
  emoji: string;
  text: string;
  danger: number;          // 打出后危险度变化，事件卡默认 +1
  effects: ExploreEffect[];
  rarity?: Rarity;
}
```

### 6.2 `src/data/maps.ts`（扩展）

```ts
export interface MapDef {
  id: string;
  name: string;
  desc: string;
  difficulty: number;
  emoji: string;

  // ── 探索模式新增 ──
  encounterPool: string[];    // 3 张遭遇卡各自从这里随机取一个 encounter id
  encounterCount: number;     // 遭遇卡张数，默认 3
  bossEncounterId: string;    // BOSS 战
  explorePool: string[];      // 探索卡 defId；重复登记 = 加权
  fillerEnemyIds: string[];   // 高危险度时追加的敌人
}
```

> 旧的 `sequence: string[]` 字段删除。CLAUDE.md 已确认**无需考虑旧存档兼容**。

### 6.3 `src/explore/`（新增，纯 TS 会话引擎）

与 `engine/` 平行的第二个纯逻辑层 —— 同样无 React、无副作用、可 `structuredClone`、可单测。

```ts
// src/explore/types.ts
export interface ExploreCard extends ExploreCardDef {
  uid: string;
}

export interface TrailEntry {
  cardId: string;
  dangerBefore: number;
  dangerAfter: number;
  note?: string;              // "残片 +12" 之类的结算摘要，供轨迹回看
}

export interface PartySnapshot {
  charId: string;
  hp: number;
  maxHp: number;
  alive: boolean;
}

export interface ExploreState {
  mapId: string;
  danger: number;
  loot: number;
  cards: Record<string, ExploreCard>;
  draw: string[];             // uid
  hand: string[];             // 事件卡（受上限约束）
  route: string[];            // 路线牌：遭遇 ×N / BOSS / 撤退
  trail: TrailEntry[];
  encountersLeft: number;
  bossRevealed: boolean;
  party: PartySnapshot[];
  pendingEncounterId: string | null;  // 已打出遭遇卡、等待战斗结算
  pendingIsBoss: boolean;
  phase: "exploring" | "inBattle" | "cleared" | "retreated" | "wiped";
  expBanked: number;
  rngState: number;
}
```

```ts
// src/explore/session.ts  —— 公开 API
createSession(mapId: string, party: PartySnapshot[], carryCards: string[]): ExploreState
playCard(s: ExploreState, uid: string): void
useAbility(s: ExploreState, ability: "scout" | "rest" | "conceal"): void
resolveBattleResult(s: ExploreState, won: boolean, survivors: PartySnapshot[], loot: number, exp: number): void
dangerTier(danger: number): DangerTier          // 数值 → 档位（唯一换算点）
encounterModifier(tier: DangerTier, isBoss: boolean): EncounterModifier
rewardMultiplier(tier: DangerTier): number
```

### 6.4 `src/explore/rules.ts`（新增）

集中所有探索层旋钮，与 `engine/rules.ts` 同惯例：

```ts
export const EXPLORE_RULES = {
  handSize: 7,
  openingDraw: 3,
  drawPerBattle: 2,
  encounterCount: 3,
  dangerPerTier: 3,
  maxTier: 5,
  abilities: {
    scout:   { danger: +1, draw: 1 },
    rest:    { discard: 2, healPercent: 0.25 },
    conceal: { discard: 2, danger: -1 },
  },
  wipe: { lootKept: 0, expKept: 0.5 },
  boss: { hpPerTier: 0.15, lootPerTier: 0.4, guardFromTier: 4 },
  tiers: [ /* 5.2 的表，逐档写 */ ],
} as const;
```

### 6.5 战斗引擎的改动（小）

```ts
// engine/types.ts
export interface AllyInit {
  // …现有字段
  startHp?: number;          // 缺省 = maxHp；探索模式传入继承的 HP
}

// 新增：危险度对遭遇战的改造
export interface EncounterModifier {
  extraEnemies?: string[];                            // 追加敌人 defId
  enemyStatuses?: { id: string; stacks: number }[];    // 全体敌人开局状态
  castTickDelta?: number;                             // 行动间隔调整（下限 1）
  bossHpMultiplier?: number;
}
```

`createBattle(encounterId, init, modifier?)` 在建局末尾套用 modifier。**引擎其余部分零改动** —— 追加敌人、开局状态、castTick 都是已有概念。

### 6.6 `src/store/exploreStore.ts`（新增）

包裹 `explore/session.ts`，与 `battleStore` 同模式（每次操作先 `structuredClone` 再交给纯函数）。

### 6.7 `src/store/townStore.ts`（扩展）

```ts
interface CharacterState {
  // …现有字段
}
interface TownStore {
  // …现有字段
  loot: number;                  // 残片余额（探索层落袋进来）
  carryCards: string[];          // 随身探索卡，最多 3 张（P2）
  bankLoot: (amount: number) => void;
}
```

---

## 七、流程与界面状态机

```
menu → town ┬→ formation
            └→ expedition ──选图──▶ explore ◀─────────────┐
                                      │                   │
                          打出遭遇/BOSS卡                  │
                                      ▼                   │
                                   battle                  │
                                      │                   │
                            ┌─────────┴─────────┐         │
                          胜利                 团灭        │
                            │                   │         │
                            ▼                   ▼         │
                         reward ────确认────────┘         │
                        (经验结算)      │                  │
                            │          │      非BOSS ──────┘
                            │          │
                       BOSS胜利      团灭
                            ▼          ▼
                         victory     defeat ──▶ town
                            │
                         撤退也走这里
```

`Screen` 枚举新增 `"explore"`。`runStore` 瘦身为「哪张地图 + 界面切换」，牌局状态全部归 `exploreStore`。

---

## 八、界面稿

```
╔══════════════════════════════════════════════════════════════════════╗
║  迷雾森林                 危险度 ▮▮▮▯▯  警戒 (7)      💠 残片 68     ║
║  ❤ 剑士 34/48   ❤ 游侠 41/41   💀 术士                              ║
╠══════════════════════════════════════════════════════════════════════╣
║                                                                      ║
║   走过的路 ──────────────────────────────────────────────▶          ║
║   ┌───┐ ┌───┐ ┌───┐ ┌───┐ ┌───┐ ┌───┐ ┌───┐        ┌╌╌╌┐          ║
║   │采 │ │⚔ │ │商 │ │采 │ │⚔ │ │掩 │ │惊 │        ┆BOSS┆          ║
║   │+1 │ │ 0 │ │+1 │ │+1 │ │ 0 │ │-2 │ │+2 │        ┆潜伏┆          ║
║   └───┘ └───┘ └───┘ └───┘ └───┘ └───┘ └───┘        └╌╌╌┘          ║
║     0     1     2     3     4     2     4  ← 危险度轨迹              ║
║                                                                      ║
╠══════════════════════════════════════════════════════════════════════╣
║  路线                    │  手牌 5/7                                 ║
║  ┌───┐ ┌───┐            │  ┌───┐ ┌───┐ ┌───┐ ┌───┐ ┌───┐          ║
║  │⚔ │ │🚪│            │  │采 │ │营 │ │焚 │ │陷 │ │掩 │          ║
║  │遭遇│ │撤退│            │  │集 │ │地 │ │巢 │ │阱 │ │埋 │          ║
║  │ ×1 │ │   │            │  │+1 │ │+1 │ │+3⚠│ │+1 │ │-2 │          ║
║  └───┘ └───┘            │  └───┘ └───┘ └───┘ └───┘ └───┘          ║
╠══════════════════════════════════════════════════════════════════════╣
║        [ 搜寻  危险+1 ]   [ 休整  弃2张 ]   [ 隐匿  弃2张 ]          ║
╚══════════════════════════════════════════════════════════════════════╝
```

### 关键交互

- **危险度仪表**：5 格档位条 + 当前数值。悬停任意手牌时，仪表**预演**打出后的位置；若会跨档，该格闪烁 + 卡面显示 `⚠ 将进入〔危险〕`
- **轨迹区**：横向滚动，右端始终可见 BOSS 卡（未揭示时为背面）。悬停已打出的卡显示当时的结算摘要
- **BOSS 卡揭示后**：卡面实时显示按当前危险度算出的血量与掉落预览，随危险度变化而跳动
- **队伍状态条**：常驻顶部，血量是玩家判断"还打不打得动"的唯一依据，必须一眼可见
- **出牌动画**：复用 `HandCard` 的出鞘离场 → 飞入轨迹区末端

---

## 九、改动清单（按文件）

### 新增

| 文件 | 内容 |
| --- | --- |
| `src/explore/types.ts` | `ExploreState` / `ExploreCard` / `TrailEntry` / `PartySnapshot` |
| `src/explore/rules.ts` | ★ `EXPLORE_RULES` —— 所有探索层旋钮（手牌上限/抽牌数/档位表/场地能力/团灭惩罚） |
| `src/explore/session.ts` | 纯 TS 会话逻辑：建局 / 出牌 / 场地能力 / 战斗回填 / 档位换算 |
| `src/explore/session.test.ts` | 单测：危险度累加与跨档、抽满即止、BOSS 入手时机、团灭结算 |
| `src/data/explore.ts` | ★ 探索卡定义表（首批 14 张 + 3 张路线牌） |
| `src/store/exploreStore.ts` | Zustand 包裹层（克隆式不可变更新） |
| `src/ui/ExploreScreen.tsx` | ★ 探索主界面 |
| `src/ui/ExploreCardView.tsx` | 探索卡卡面（结构与战斗卡不同，不复用 `CardView`） |
| `src/ui/DangerMeter.tsx` | 危险度仪表（含悬停预演与跨档警示） |
| `src/ui/TrailStrip.tsx` | 轨迹区 |

### 修改

| 文件 | 改动 |
| --- | --- |
| `src/data/maps.ts` | `sequence` → `encounterPool` / `encounterCount` / `bossEncounterId` / `explorePool` / `fillerEnemyIds` |
| `src/data/encounters.ts` | 补 BOSS 遭遇战与填充敌人的组合 |
| `src/data/index.ts` | 加 `getExploreCard` / `makeExploreCard` |
| `src/engine/types.ts` | `AllyInit.startHp`、新增 `EncounterModifier` |
| `src/engine/battle.ts` | `createBattle` 接受并套用 `EncounterModifier`；`startHp` 生效 |
| `src/store/battleStore.ts` | `init` 透传 modifier |
| `src/store/runStore.ts` | 瘦身：`index`/`sequence` 相关逻辑移出；新增 `screen: "explore"` 流转；战斗结束回填 HP 与战利品给 `exploreStore` |
| `src/store/townStore.ts` | 新增 `loot` / `bankLoot`；P2 加 `carryCards` |
| `src/ui/ExpeditionScreen.tsx` | 选图后进 `explore` 而非 `battle` |
| `src/App.tsx` | 加 `explore` case |
| `src/ui/transitions.ts` | 配 `explore>battle` / `battle>explore` 的过场（战斗进出应有明显的"下潜/上浮"感） |
| `src/ui/EndScreen.tsx` | 展示轨迹回顾 + 战利品结算 + 结束方式（通关/撤退/团灭） |
| `src/ui/ambience.ts` | 危险度档位 → 调色强度 |
| `src/styles.css` | 探索界面全套样式 |
| `README.md` | 目录结构 + 文件明细 + 设计方案章节 |

---

## 十、分期实施

### P0 · 最小闭环 ✅ 已完成

1. ✅ `explore/` 三个文件（types / rules / session）+ 21 条单测
2. ✅ `data/explore.ts` 首批 14 张卡（「旅行商人」暂缓，等卡型设计定案）
3. ✅ `maps.ts` 结构改造，三张地图全部改造完毕
4. ✅ 引擎 `AllyInit.startHp` + `EncounterModifier`（追加敌人 / 开局状态 / castTick / HP 倍率）
5. ✅ `ExploreScreen` 全套：危险度仪表（含跨档预演）、队伍血量、轨迹、路线/手牌、三个场地能力、弃牌选择
6. ✅ 打通 explore ↔ battle 往返，通关 / 撤退 / 团灭三种收场

**已知偏差**：BOSS 遭遇战目前复用已有的 `e3` / `n1`，尚无专属 BOSS 敌人——等正式内容替换。

### P1 · 手感与表现

轨迹区动画、危险度悬停预演与跨档警示、BOSS 强度实时预览、档位联动氛围调色、过场特效、`EndScreen` 轨迹回顾。

### P2 · 内容与深度

随身卡（城镇配置 + 编队界面入口）、完整商店界面、其余地图的专属卡池、更多事件卡型（连锁事件、需要在后续做选择的卡）。

---

## 十一、待定 / 开放问题

1. **残片的城镇用途**：目前只进不出。需要一个消耗口 —— 建议接到 `TownScreen` 的占位设施（补给站买消耗品 / 锻造台升级卡牌），否则玩家攒了没意义。
2. **团灭惩罚力度**：3.7 的 `lootKept: 0 / expKept: 0.5` 是保守起点。跑通后如果发现玩家毫无顾忌地冲高危险度，就往上加（引入「重伤」状态需城镇治疗）。
3. **遭遇卡是否可以指定顺序**：目前 3 张遭遇卡内容在开局就随机确定并显示在卡面上，玩家可以选先打哪一场（比如先打软的攒资源）。这是**额外的一层决策**，建议保留。
4. **危险度是否应设上限**：现为 12+ 封顶第 5 档但数值继续累加。若后续加入「按数值线性缩放」的效果，需要重新考虑封顶。
5. **多张地图的差异化**：卡池是唯一的性格来源。第二张地图（霓虹城市）应该走完全不同的味道 —— 比如商人多、采集少、有「情报」类卡可以揭示 BOSS 意图（呼应战斗层的「洞察」）。

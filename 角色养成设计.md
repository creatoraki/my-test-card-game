# 角色养成 · 设计文档

> 状态：**设计定稿，待实现**。
> 目标：把现在单薄的「等级 + 属性点 + 个人卡组」扩展成一套完整的养成结构 —— 一局 12 周的战役骨架、角色的三条成长轴（特性 / 卡组 / 装备）、以及跨局的解锁与传承积分。
>
> 前置阅读：`探索模式设计.md`（本文档的「一次出击」= 那份文档里的一整场探索牌局）。

---

## 一、设计目标与核心张力

### 1.1 一句话

> **时间是唯一真正稀缺的资源。每一周花在支线上，都是从推倒最终 BOSS 的额度里扣的。**

一局游戏 = **12 周 = 12 次出击**。每周必须出击一次（不能跳过），出击目标二选一：

- **主线地图**：常驻可见，共 6 段，打穿一段推进 1 格进度。
- **支线地下城**：不推进主线，用来赚积分、捡装备、扩卡组 —— 也就是「变强」。

12 周结束仍未击杀最终 BOSS ⇒ **本局失败**。

### 1.2 为什么这个骨架成立

12 周的账：

```
最快通关 = 6 周（每周都推主线，零支线）   → 阵容极弱，大概率死在后段
理论最慢 = 12 周（前 6 周纯刷支线）        → 阵容超强，但零容错
实际玩法 = 在这 6 周的自由额度里，赌自己需要多少准备
```

**一半时间被主线锁死、一半时间自由支配**。玩家每一周都在回答同一个问题：**「我现在够强了吗？」**

这个骨架顺手修好了探索层一个悬着的问题：原先「撤退」的代价只是丢一堆抽象数字，其实不太疼。有了周历之后，撤退 = 烧掉一整周 = 烧掉 1/12 的总时间预算。`探索模式设计.md` 里那句「这一段值不值得我再烧一档净化粒子」，背后终于压上了真正的筹码。

### 1.3 主线与支线的失败代价是不对称的 —— 核心张力

| | 打赢 | 撤退 | 团灭 |
| --- | --- | --- | --- |
| **支线** | 战利品 + 熟练度 + 一周 | 战利品 + 熟练度 + 一周 | 熟练度 + 一周 |
| **主线** | **进度 +1** + 战利品 + 一周 | 白烧一周 | 白烧一周 |

支线怎么打都有收成；**主线是二元的 —— 不打穿就等于这一周凭空蒸发**。

这条不对称自动生成了整局最核心的判断：**「我够不够强，敢不敢去碰主线」**。不需要任何额外机制去制造这个决策，它是结构自带的。

### 1.4 为什么养成资产属于「一局」，而不是账号

设计目标**不是**让玩家用上所有角色，而是：**选定几个角色自由组合，专注培养这一套；想试别的组合，重开一局。**

推论（这几条决定了整套架构）：

- 队伍在**开局时锁定**，局内不可换人 —— 能随时换人的话，「组合」就不是承诺，专注培养也就无从谈起。
- 角色养成资产（卡组 / 装备 / 特性 / 熟练度）**随本局结束一并清空**。
- 跨局留下来的只有**「可能性」**：解锁新角色 / 新卡 / 新装备 / 新地图，以及由解锁量派生的**传承积分**。
- ⇒ 现在的 `townStore` 把「永久资产」和「本局进度」混在一个 `persist` 里，**必须拆开**（见第七节）。

### 1.5 重复可玩性来自组合，不来自单角色深度

5~8 个角色里选 3 个 ≈ 56 种组合。**这 56 种组合就是重复可玩性的来源**，而不是「每个角色单独有多深」。

推论很硬：**角色的特色不能只是「我自己很强」，必须是「我和谁在一起会变得不一样」。** 否则 56 种组合会退化成「选伤害最高的 3 个」，一局就玩腻了。详见第五节。

---

## 二、名词表

| 名词 | 含义 |
| --- | --- |
| **战役 / Campaign** | **一局**游戏，12 周，从开局配置到通关或失败。养成资产的生命周期单位 |
| **出击 / 周** | 一次远征 = 一周。无论打赢、撤退还是团灭，都消耗一周 |
| **主线地图** | 常驻可见的战役目标，分 6 段，打穿一段推进 1 格 |
| **支线地下城** | 不推进主线的出击目标，用来换取成长资源 |
| **特性 / Trait** | 角色专属被动，开局从 2~3 条里选定 1 条，不可拆卸、不可改造 |
| **词条 / Affix** | 装备上的规则性效果（区别于纯数值） |
| **熟练度** | 原「经验」。战后**即时入账**，死了也拿得走。用于升特性、解锁进阶卡 |
| **战利品** | 探索层产出的**实物**（废件 / 材料 / 装备 / 数据存档），占背包格，**活着回城才落袋** |
| **城市居民积分** | 唯一城镇通货。来自控制终端工单 + 把战利品卖给售货机。用于抽卡、买装备、扩卡组容量 |
| **元档案 / Meta** | 跨局永久保存的解锁集合与统计 |
| **传承积分** | 由已解锁内容**派生**出的数值（不是货币），开局用于配置启动器 |
| **启动器 / Starter** | 开局用传承积分点亮的强力功能（初始装备 / 初始资源 / +1 周…） |

---

## 三、一局的完整流程

```
开局配置 ──▶ 第 1 周 ──▶ … ──▶ 第 12 周 ──▶ 结算 ──▶ 解锁入账 ──▶ 开下一局
   │                │
   │           ┌────┴────┐
   │           ▼         ▼
   │        主线出击   支线出击
   │           │         │
   │           └────┬────┘
   │                ▼
   │          探索牌局（见 探索模式设计.md）
   │                ▼
   │          回城 → 花积分养成 → 下一周
   │
   └─ 选角色（锁定队伍）+ 花传承积分配启动器 + 每人选 1 条特性
```

### 3.1 开局配置

整局最重要的一次决策，值得单独一个界面（比现有 `FormationScreen` 权重更高）：

1. **选队伍**：从已解锁角色里选 3 人（`RULES.progression.partySize`）。选完**本局锁定**。
2. **每人选 1 条特性**：从该角色的 2~3 条里选，**本局不可更改**（熟练度会投进去，天然形成沉没成本）。
3. **配启动器**：用传承积分在 3 个槽里配置（见第六节）。

界面必须同时展示每个角色的**特性文本**和**协同标签**（见 5.2）—— 玩家还没玩过就要选，能拿来判断的只有一句话能说清的东西，不可能是 10 张卡。

### 3.2 周循环

```
出击选择界面（周历）
  ├─ 主线地图卡片：常驻，显示当前段数 / 已恶化程度 / 剩余周数警告
  └─ 支线地下城卡片：若干张，部分带「限时」标记
        ↓ 选定
   探索牌局（现有 explore/ 全套，不改）
        ↓ 通关 / 撤退 / 团灭
   回城结算：背包落袋（卖出换积分）、熟练度入账、主线进度判定
        ↓
   城镇养成阶段：抽卡 / 买装备 / 强化 / 扩容
        ↓
   week += 1
```

**每次出击开始时，全员满血归队。** 死亡只在一次出击内有效 ⇒ 血量损耗和阵亡都被关在一次探索牌局内部，周与周之间不留伤疤。这与现有 `PartySnapshot`（血量只在一场牌局内继承）完全吻合，**`campaignStore` 不需要记录角色血量，也不需要重伤/休养系统**。

### 3.3 主线推进与「恶化」

**主线 6 段，每次出击最多推进 1 段。** 打穿该段 BOSS ⇒ `mainProgress += 1`；撤退或团灭 ⇒ 进度不变。

⚠ **必须让拖延本身有代价，而不只是机会成本。** 否则玩家玩三局就会算出「最优是前 N 周刷支线」，之后每局照抄，时间管理的决策就死了。

解法：**主线随周数恶化**。三种手段（可叠加）：

| 手段 | 说明 | 落点 |
| --- | --- | --- |
| **BOSS 累进强化** | 每过 2 周，最终 BOSS 获得一层永久强化 | `EncounterModifier` 的 HP 倍率 + 开局状态 |
| **段落沦陷** | 主线某段在第 N 周后成分变差（`castTick` 全体 -1、追加敌人） | `EncounterModifier` |
| **限时机会** | 某段落在第 7 周前通过可额外救下一个角色/装备，之后永远错过 | 支线/主线的可选目标 |

玩家的算式因此从「我要刷够多强」变成 **「我强化的速度，跑不跑得赢它恶化的速度」** —— 这是一场赛跑，赛跑没有固定最优解。

> **实现上零成本**：`EncounterModifier` 已经支持「追加敌人 / 全体开局状态 / `castTick` 调整 / HP 倍率」四条改造，净化粒子档位是它的第一个来源，**「周数」直接作为第二个来源接进去即可，引擎侧一行不用改**。

配套手段：**部分支线按周开放**（第 3~5 周才出现），制造「这周必须去，不然错过」的具体冲突，而不是抽象的机会成本。

### 3.4 胜负判定

| 结局 | 条件 |
| --- | --- |
| **通关** | 击杀主线第 6 段的最终 BOSS |
| **失败（超时）** | 第 12 周结束仍未通关 |
| **失败（提前判定）** | `12 - week + 1 < 6 - mainProgress` —— 剩余周数已经装不下剩余段数 |

**提前判定是必需项，不是优化项。** 主线容错只有 6 次；如果玩家翻车两三次，本局在数学上已经死了却还剩五六周必须走完 —— 明知没戏还得打，是最劝退的体验。系统随时算这个比较，一旦成立就结束本局、直接进结算，**该给的解锁照给**。

第二期可加软化手段：**主线失败保留「已探明」** —— 撤退/团灭时记下打到哪，下次重打这一段敌人减少或起始净化粒子更充裕。螺旋被软化成「多花一周补考」。（需要为主线段落多做一套进度状态，故放到 P2。）

---

## 四、角色的三条轴

### 4.1 为什么是三条

卡组和装备的**形状是重复的** —— 两者都是「从池子里挑东西装上去」的加法式外挂，都可以被替换、被改造。它们回答的都是「我装了什么」，没有一条回答**「我是谁」**。

```
角色 = 裸体面板（characters.ts 固定，本局永不变）
     + 特性（2~3 选 1，不可拆卸）    ← 「我是谁」     熟练度
     + 卡组（有容量上限）             ← 「我能做什么」  积分抽卡
     + 装备（3 槽，队内自由拆卸）     ← 「我强在哪」    积分打造
```

三条轴各回答一个不同的问题，互不重复，且分别对应城镇的三个设施（酒馆 / 编队 / 锻造台）—— `TownScreen.LOCKED_FACILITIES` 里那几个占位刚好各就各位。

### 4.2 特性（Trait）

**每角色 2~3 条，开局选定 1 条，本局不可更改。**

它是性价比最高的辨识度来源：玩家记不住 10 张卡，但**记得住一句话**。而且选队伍发生在开局之前，玩家能拿来判断的只有这句话。

特性应当挂在本作已有的四个机制上（时刻 / 光 / 仇恨 / 洞察），示例：

| 角色 | 特性 | 挂在哪 |
| --- | --- | --- |
| 剑士 | **锋回**：每回合第一张普通牌不推进时刻 | 时刻 |
| 剑士 | **蓄势**：时刻 ≥4 时，你的伤害 +40% | 时刻 |
| 重装 | **磐石**：仇恨恒定 ×1.5，受到的伤害 -2 | 仇恨 |
| 观测者 | **窥破**：战斗开始时对所有敌人施加 1 层「洞察」 | 洞察 |
| 技师 | **超载**：每回合第一张速攻牌不耗光 | 光 |

**成长方式**：用熟练度把激活的那条特性升到 2 级 / 3 级（数值加强或追加一小段效果）。熟练度另一个用途是解锁该角色卡池里的**进阶卡**。

### 4.3 卡组

沿用现有的**个人卡组**结构（`CharacterState.deck`，战斗卡组 = 上阵角色个人卡组的合集），但补上一条关键约束：

> **卡组有容量上限。**

现在卡只能加不能删，剑士 3 张起手，抽到 15 张时抽到关键牌的概率反而更低 —— 这是爬塔类的经典陷阱，抽卡的爽感会在中期转成负担。

- **起始容量 8**，可在城镇花积分扩容，**上限 12**（每次 +1，价格递增）。
- **满员后抽卡强制二选一**：新卡换掉旧卡中的一张。⇒ 抽卡从「加一张」变成 **「换掉哪一张」**，决策密度直接翻倍，而且不需要额外做「移除卡牌」的设施。
- 抽卡来源仍是角色**专属卡池** `poolCardIds`，但消耗从属性点改为**城市居民积分**。

### 4.4 装备

**3 个槽位（武器 / 护甲 / 饰品），开局全开**（等级已删，不再另设闸门）。装备**入本局仓库，队内 3 人自由拆卸** —— 一件装备掉了给谁穿，本身就是一次决策。

**装备 = 少量四维 + 1 条规则词条。** 纯数值装备很便宜但很无聊；词条才是让玩家「重新想一遍卡组怎么搭」的东西。三个槽位各有性格：

| 槽位 | 性格 | 词条示例 |
| --- | --- | --- |
| **武器** | 进攻 | 锋锐：本人 DAMAGE 效果 +2 ｜ 破甲：伤害无视目标 5 点护盾 ｜ 连击：本回合第 2 张牌伤害 +50% |
| **护甲** | 生存 / 仇恨 | 壁垒：回合开始获得 3 护盾 ｜ 荆棘外壳：受击时对攻击者施加 1 层荆棘 ｜ 威压：初始仇恨 +8 |
| **饰品** | 资源 / 规则 | 余光：每回合第一张速攻牌不耗光 ｜ 预读：回合开始额外抽 1 张 ｜ 猎手：对被「洞察」的目标伤害 +50% |

饰品那类才是特色所在 —— 它们直接改的是本作最独特的三个机制（时刻 / 光 / 洞察）的规则。

**品质 3 档**（常见 / 精良 / 稀有），影响四维数值与词条强度。来源：敌人掉落表 + BOSS 掉落（品质按 `探索模式设计.md` §5 的掉落系数 `K`）+ 锻造台花积分打造 + 少量终点事件。

### 4.5 ★ 关键实现思路：特性与词条都做成「永久状态」

**不要为特性和词条各建一套新的引擎子系统。**

`statuses.ts` 已经有一套完整的行为钩子机制：`onRoundStart` / `onRoundEnd` / `onTick` / `modifyOutgoingDamage` / `modifyIncomingDamage` / `onAfterAttacked`，并通过 `ctx.ops` 反向调用引擎原语（避免循环依赖）。

⇒ **特性和装备词条，实现为「开战时自动施加、不可驱散、不显示层数的永久状态」即可。**

- `battle.ts` 的 `createBattle` 在 `startRound` **之前**给每个 Ally 施加其特性状态 + 三件装备的词条状态（时机与 `EncounterModifier` 的开局状态一致，理由同样是意图预览要吃到加成）。
- 引擎侧改动几乎为零，新增一条特性/词条 = 在注册表里加一个带钩子的 `StatusDef`。
- 代价：`StatusDef` 需要加一个 `hidden`/`permanent` 标记，让 `StatusPips` 不把它们混进普通 BUFF 排（或单独用一排小图标展示）。

这一条决定了装备与特性的实现成本能不能压住，**动手前务必按这个思路走**。

### 4.6 被删掉的东西

| 删除 | 理由 |
| --- | --- |
| **等级** | 纯数字，没有承载任何决策。升级的爽点应该来自「能做新的事」，不是面板变大 |
| **属性点 / 四维自由加点** | ① 一种通货两个出口（加四维 / 抽卡），玩家算出最优解后决策消失；② `+3 HP` 这种线性小数值加 20 点也不会让人觉得角色变得不一样；③ 与装备的职能完全重复 |

⇒ **四维 = `characters.ts` 基础值 + 装备**，`deriveStats` 的换算从「基础 + 加点 × 每点收益」改为「基础 + 装备合计」，仍是唯一换算点。装备因此成为唯一面板来源，权重足够重。

⇒ **经验保留，但改名「熟练度」并换个身份。** ⚠ 这一条是承重结构：`探索模式设计.md` 写着「经验即时入账，实物战利品要活着回城才落袋 —— 这个不对称是撤退成为真选择的唯一原因」。如果养成全靠会丢的东西，往前推就是纯赌博、没有保底，探索层的核心张力会塌一块。

| 通货 | 结算时机 | 用途 |
| --- | --- | --- |
| **熟练度** | 战后即时入账，**死了也拿得走** | 升特性、解锁进阶卡 |
| **实物战利品** | **活着回城才落袋**，团灭全丢（已寄回的除外）；占背包格 | 直接装备、分解、卖出换积分 |
| **城市居民积分** | 回城后由工单与卖出结算，**存量不会因团灭消失** | 买装备、强化、扩卡组容量、抽卡 |

叙事上甚至比原来更顺：**「打赢的仗一定让这个角色更懂自己，但背包里的东西随时可能全丢。」**

> 积分之所以不随团灭消失，是因为它已经落在据点里；会丢的是**这一趟背在身上的实物**。风险与保底的分界线因此非常直观。

---

## 五、角色协同 —— 真正的设计重心

### 5.1 原则：有些卡应该在单人时是废牌

这是让「组合」真正成为决策的唯一办法。如果每张卡单独都够用，组合就只是加法，不是化学反应。

### 5.2 四条协同线

全部挂在已有机制上，不需要任何新系统：

| 机制 | 角色 A | 角色 B | 化学反应 |
| --- | --- | --- | --- |
| **时刻** | 大量速攻牌，把时刻压在前段 | 卡牌「时刻 ≥4 时效果翻倍」 | A 压时刻 vs B 要时刻 → **反向拉扯**，玩家得学会分段出牌 |
| **仇恨** | 高仇恨坦克，把敌人全拉过来 | 「当你不是任何敌人的目标时，伤害 +50%」 | 没有 A，B 就是废牌；有 A，B 就是本局核心输出 |
| **光** | 每回合产 1 点光，自己没输出 | 一堆 4~5 费大招 | A 单独玩毫无意义，配 B 直接改变整局节奏 |
| **洞察** | 唯一能稳定施加「洞察」的角色 | 「对被洞察的目标伤害翻倍」+ 意图可见 | 把「意图默认不可见」这条核心设定变成一条**可选的构筑路线** |

开局选人界面上，每个角色应带 1~2 个**协同标签**（`压时刻` / `吃时刻` / `拉仇恨` / `脱离仇恨` / `产光` / `耗光` / `施加洞察` / `吃洞察`），让玩家不玩也能看懂谁配谁。

### 5.3 角色阵容规划（5~8 人）

建议按「协同标签」而不是按「职业」来规划名额，保证任意两条线都有至少两个角色能接上：

| # | 定位 | 主标签 | 备注 |
| --- | --- | --- | --- |
| 1 | 剑士 | 压时刻 / 均衡 | 初始角色，已实现 |
| 2 | 重装 | 拉仇恨 | 初始角色 |
| 3 | 观测者 | 施加洞察 | 初始角色 |
| 4 | 刺客 | 脱离仇恨 / 吃洞察 | 解锁 |
| 5 | 技师 | 产光 | 解锁 |
| 6 | 术士 | 耗光 / 吃时刻 | 解锁 |
| 7~8 | 待定 | 跨线混合 | 解锁，后期内容 |

**开局默认只解锁 1~3 号**（唯一组合），随跨局解锁逐步开到 8 人 ⇒ 可选组合数从 1 种长到 56 种。**成长感直接体现在「我现在有更多组合可以试」上，且完全不破坏局内平衡。**

---

## 六、跨局：解锁与传承积分

### 6.1 传承积分是「派生值」，不是货币

> 积分总额 = f(已解锁内容)，**只增不减**；开局分配一次，本局用掉，**下一局又是满额**。

这是**预算制**，不是消耗品。重复通关不解锁新东西 ⇒ 积分不涨 ⇒ 玩家想变强只能去碰没碰过的内容。

⚠ **硬性要求：积分必须现算，不能存字段。**

```ts
// metaStore 只存解锁集合，积分永远是它的投影
export function computeLegacyPoints(unlocked: UnlockSet): number;
```

一旦把积分当字段存起来，早晚会出现「存的数」和「实际解锁集合」对不上的 bug（补发解锁、调整积分价值、修复存档时都会漂）。**解锁集合是唯一真相。**

### 6.2 槽位制启动器 —— 防溢出的关键

⚠ 解锁越多 → 积分越多 → 开局越强。到后期玩家解锁了八成内容，开局就能拿满装备满资源，**难度曲线会整个塌掉** —— 而这恰恰发生在玩家最想认真玩的时候。

**别让积分直接换数量。** 采用**槽位 + 预算**双旋钮：

- **启动器槽位固定少（3 格）**，格数本身也是一个可解锁项（1 格 → 3 格）；
- **积分决定你能选多贵的**：每个启动器标价，在 3 个槽里塞总价 ≤ 预算的组合。

⇒ 解锁再多也只能带 3 个启动器，**积分增长带来的是「选更强的单品」而不是「带更多东西」，永远不会爆炸**。而且「3 个槽怎么配」本身就是一次有意思的开局决策。

**启动器品类**：

| 品类 | 示例 | 定价 |
| --- | --- | --- |
| 初始资源 | 开局获得 300 积分 | 低 |
| 初始装备 | 开局获得一件随机精良装备 | 中 |
| 初始卡 | 每人开局多带 1 张专属卡 | 中 |
| 特性起点 | 开局特性直接 2 级 | 中高 |
| **+1 周** | 本局 13 周 | **最高** —— 直接给核心稀缺资源 |
| **带代价的高价品** | 开局一件稀有武器，但全场敌人 `castTick` -1 | 高，作为后期积分的泄压阀 |

最后一类很重要：积分多到花不完时，它让玩家主动去换更刺激的局面，而不是躺赢。

### 6.3 解锁表

解锁项定义在新增的 `data/unlocks.ts`，**积分价值按内容分量给，不要一律 +1**（解锁一个新角色显然比解锁一张普通卡值钱）。

解锁条件天然挂在周历上 —— 每一条都在逼玩家换一种打法：

| 条件示例 | 解锁 |
| --- | --- |
| 首次通关 | 角色 #4、支线地图 ×1 |
| **8 周内**通关 | 角色 #5、启动器槽位第 2 格 |
| **零支线**通关（6 周全推主线） | 稀有装备图纸 |
| 通关时**全员存活** | 角色 #6 |
| 第 7 周前完成某限时支线 | 进阶卡包 |
| 在净化粒子第 5 档（枯竭）下通关 BOSS | 启动器槽位第 3 格 |

---

## 七、数据结构

### 7.1 三层持久化架构

现在的 `townStore` 把「永久资产」和「本局进度」混在一个 `persist` 里（key `town-profile-v1`），**必须拆成三层**：

| 层 | 存什么 | 生命周期 | 落点 |
| --- | --- | --- | --- |
| **元档案** | 解锁集合、通关统计（积分派生不存） | 永久 | **新增 `metaStore`**（persist `meta-v1`） |
| **战役档案** | 周数、主线进度、队伍、特性、卡组、装备仓库、积分、熟练度、启动器 | 一局，开新局即清 | **`townStore` → `campaignStore`**（persist `campaign-v1`） |
| **远征档案** | 探索牌局状态 | 一次出击，关页面即作废 | 现 `exploreStore`，**不动** |

⚠ **命名**：现有的 `runStore` 指「一次远征的流程编排」，与「一局」概念冲突。约定：**一局 = campaign（战役）**，**一次出击 = run / expedition**。故 `townStore` 改名 `campaignStore`，`runStore` 保持原义不动。

### 7.2 `src/store/metaStore.ts`（新增）

```ts
export interface UnlockSet {
  characters: string[];
  cards: string[];
  equipment: string[];
  maps: string[];
  traits: string[];
  starterSlots: number;   // 1~3
  starters: string[];     // 已解锁的启动器 id
}

interface MetaStore {
  unlocked: UnlockSet;
  campaignsPlayed: number;
  campaignsCleared: number;
  bestWeeks: number | null;        // 最快通关周数
  grantUnlocks: (ids: string[]) => string[];  // 返回本次真正新增的（用于结算页高亮）
  resetAll: () => void;            // 「清空所有数据」，与「放弃本局」区分
}

export function computeLegacyPoints(u: UnlockSet): number;  // 纯函数，不存
```

### 7.3 `src/store/campaignStore.ts`（由 `townStore` 改造）

```ts
export interface CharacterState {
  charId: string;
  traitId: string;          // 开局选定，本局不可改
  traitLevel: number;       // 1~3，熟练度升
  mastery: number;          // 熟练度（原 exp）
  deck: Card[];
  deckCapacity: number;     // 起始 8，花积分扩容，上限 12
  equipped: { weapon: string | null; armor: string | null; trinket: string | null };
  pendingDraw: string[] | null;
  pendingReplace: string | null;  // 卡组满员时待替换的新卡
}

interface CampaignStore {
  week: number;              // 1..12
  mainProgress: number;      // 0..6
  party: string[];           // 开局锁定
  characters: Record<string, CharacterState>;
  inventory: string[];       // 本局装备仓库（未装备的）
  credits: number;           // 城市居民积分（唯一城镇通货）
  starters: string[];        // 本局启用的启动器
  outcome: 'ongoing' | 'cleared' | 'failed' | null;

  startCampaign: (party, traits, starters) => void;
  advanceWeek: () => void;         // 出击结束后 +1，并做胜负判定
  grantMastery: (charIds, amount) => MasteryGain[];
  spendShards / buyEquipment / equip / unequip / expandDeck / startDraw / pickDraw / replaceCard / upgradeTrait
  abandonCampaign: () => void;     // 放弃本局（≠ 清空所有数据）
}
```

⚠ 现有 `toggleParty` 的自由换人**必须移除**（队伍开局锁定）。`resetProfile` 分裂成**「放弃本局重开」**（清战役档案，保留元档案）与**「清空所有数据」**（连元档案一起清）—— 现在只有一个按钮，会误伤。

### 7.4 新增数据文件

| 文件 | 内容 |
| --- | --- |
| `data/traits.ts` | `TraitDef[]`：id / 归属角色 / 名称 / 三级文本 / 对应的 `StatusDef` id |
| `data/equipment.ts` | `EquipmentDef[]`：id / 槽位 / 品质 / 四维加成 / 词条 id / 打造价 |
| `data/affixes.ts` | 词条 → `StatusDef` 的注册（或直接并进 `engine/statuses.ts`） |
| `data/unlocks.ts` | `UnlockDef[]`：id / type / 解锁条件 / 积分价值 |
| `data/starters.ts` | `StarterDef[]`：id / 名称 / 标价 / 生效方式 / 可选的代价 |

### 7.5 现有文件的扩展

| 文件 | 改动 |
| --- | --- |
| `data/maps.ts` | `MapDef` 加 `kind: 'main' \| 'side'`；主线加 `stages: MapStageDef[]`（每段一套 `encounterPool`/`boss`/`explorePool`）；支线加可选 `availableWeeks: [number, number]` |
| `data/characters.ts` | 加 `traitIds: string[]`（2~3 条）、`synergyTags: string[]`；`maxHp`/`threat` 保留为裸体面板 |
| `engine/rules.ts` | `progression` 段重写：删 `levelUpPoints`/`expBase`/`expGrowth`/`*PerPoint`/`drawCost`；加 `campaign`（`totalWeeks: 12`、`mainStages: 6`、`escalateEveryWeeks: 2`）、`deck`（`startCapacity: 8`、`maxCapacity: 12`、扩容价曲线）、`mastery`（升特性所需）、`shards`（各项定价） |
| `engine/statuses.ts` | `StatusDef` 加 `permanent?: boolean` / `hidden?: boolean`；注册特性与词条状态 |
| `engine/battle.ts` | `createBattle` 在 `startRound` 前施加特性 + 词条状态 |
| `store/runStore.ts` | `launchBattle` 组队时读 `campaignStore`；`resolveBattle` 发**熟练度**而非经验；出击结束调 `campaignStore.advanceWeek` |

---

## 八、界面

### 8.1 新增 / 改造的界面

| 界面 | 状态 | 说明 |
| --- | --- | --- |
| `CampaignSetupScreen` | **新增** | 开局配置：选队伍 + 选特性 + 配启动器。整局最重要的决策界面 |
| `WeekScreen` | **由 `ExpeditionScreen` 改造** | 周历 / 出击选择。主线卡片常驻 + 支线卡片若干 |
| `FormationScreen` | **大改** | 删四维加点；装备槽接真实数据 + 仓库；卡组显示 `n/容量`；抽卡改花积分；满员时走替换流程 |
| `ForgeScreen` | **新增** | 锻造台：花积分打造/强化装备、扩卡组容量 |
| `TownScreen` | 小改 | 解锁锻造台；显示第 N/12 周、主线进度、积分余额；重置按钮拆成两个 |
| `EndScreen` | 中改 | 增加**战役结算**形态：通关/超时/提前判定，本局新增解锁高亮，积分变化 |

### 8.2 周历界面稿

```
┌──────────────────────────────────────────────────────────────┐
│  第 5 / 12 周            主线进度 ●●○○○○ (2/6)      积分 420 │
│  ⚠ 剩余 8 周，还需推进 4 段 —— 余量 4 周                     │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│   ┏━━━━━━━━━━━━━━━━━┓   ┌──────────┐ ┌──────────┐          │
│   ┃ 【主线】 深渊之喉 ┃   │ 锈蚀矿坑 │ │ 霓虹城市 │          │
│   ┃  第 3 段 / 共 6 段┃   │  ★★☆☆☆  │ │  ★★★☆☆  │          │
│   ┃  ⚠ 已恶化 ×2      ┃   │ 装备掉落↑│ │ ⏳ 限第5周│          │
│   ┃  打穿 → 进度 +1   ┃   │          │ │          │          │
│   ┃  撤退 → 白烧一周  ┃   └──────────┘ └──────────┘          │
│   ┗━━━━━━━━━━━━━━━━━┛                                       │
│      ↑ 常驻，永远在最左，永远看得见                          │
└──────────────────────────────────────────────────────────────┘
```

关键点：

- **主线卡片常驻最左、视觉最重**，「每周都能看见他」是叙事本身；它的描述文字/配图随周数变化 ⇒ **零成本的叙事框架**（第 3 周它长出了新东西，第 8 周它开始渗出）。
- 顶部那行**余量提示**是整个界面的心跳。余量 ≤1 时应该变红并加重。
- 主线卡片必须把「撤退 = 白烧一周」写在脸上，这是玩家做决策的核心信息。

---

## 九、分期实施

依赖顺序是硬的：**必须先有「一局」，才谈得上「跨局」。**

### P0 · 战役骨架（不含新养成轴）

先把「局」跑通，养成暂时沿用现有的属性点体系。

1. `campaignStore`（由 `townStore` 改造）：`week` / `mainProgress` / `outcome` / `advanceWeek` / 胜负判定（含提前判定）
2. `MapDef.kind` + 主线分段；`WeekScreen`（周历）
3. 队伍开局锁定；`resetProfile` 拆成两个按钮
4. `EndScreen` 增加战役结算形态

**可验证**：能完整玩一局 12 周，能通关，能失败，能重开。

### P1 · 装备

5. `StatusDef` 加 `permanent`/`hidden`；`createBattle` 施加词条状态
6. `data/equipment.ts` + `data/affixes.ts`；BOSS 掉落 + `ForgeScreen` 打造
7. **删四维自由加点**，`deriveStats` 改为「基础 + 装备」；积分成为主要通货
8. `FormationScreen` 装备槽接真实数据 + 仓库

**可验证**：装备能穿脱、词条在战斗里真的生效、积分有地方花。

### P2 · 卡组容量 + 特性

9. `deckCapacity` + 满员替换流程；扩容在锻造台
10. `data/traits.ts` + 熟练度（经验改名 + 换用途）+ 特性升级
11. `CampaignSetupScreen` 的选特性环节
12. **主线恶化**（`EncounterModifier` 接周数）+ 主线「已探明」软化机制

**可验证**：同一个角色能用不同特性玩出不同流派；拖延有真实代价。

### P3 · 跨局

13. `metaStore` + `data/unlocks.ts` + `computeLegacyPoints`
14. `data/starters.ts` + 槽位制启动器 + `CampaignSetupScreen` 的配置环节
15. 结算页的解锁高亮与积分变化

**可验证**：通关后能解锁东西，下一局开局确实更强，重复通关不涨积分。

### P4 · 内容铺量

16. 角色 #2~#8 + 各自的特性/卡池/协同标签
17. 支线地下城若干 + 限时开放
18. 装备词条铺量

---

## 十、待定 / 开放问题

| # | 问题 | 现状 |
| --- | --- | --- |
| 1 | 队伍是否固定 3 人？还是允许 2 人（换取某种收益）？ | 暂定固定 3，`AllyBar` 已按 4 格设计，留了余地 |
| 2 | 删掉四维自由加点是否太狠？ | 可逆：保留 `CharacterAttrs` 结构，只是不再提供加点入口。若装备撑不起面板成长，随时能加回来 |
| 3 | 主线 6 段 / 12 周的比例 | 起点值，需要实测。旋钮在 `RULES.campaign` |
| 4 | 一局时长 | 支线远征应刻意做短（`routeSegments` 更少 → 5 段，主线 7 段），主线做重。目标：一局 ≤ 3 小时 |
| 5 | 熟练度是否只有上阵角色吃 | 队伍开局锁定 ⇒ 没有板凳，此问题自动消失 |
| 6 | 特性是否允许局内花积分重选 | 暂定不可改（沉没成本是特性的一部分）。若测下来太硬，开一个「每角色每局限 1 次」的口子 |
| 7 | 装备是否需要「诅咒」类负面词条 | 与启动器的「带代价高价品」是同一思路，P4 再说 |
